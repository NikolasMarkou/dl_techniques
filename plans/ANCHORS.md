# Decision Anchor Manifest
*Append-only. One line per anchored decision: `<plan-id>/D-NNN | close-date | rationale`. Keeps a `# DECISION <plan-id>/D-NNN` source comment resolvable after its plan directory is gone.*

plan-2026-08-04T044628-4c240b4c/D-005 | 2026-08-05 | Copy the one-line never-narrow ternary inline (site 3 by `grep -rn work_dtype src/`), do NOT promote to a shared helper yet, and store the cos/sin angle table as a config-derived NumPy constant rather than an `add_weight` variable.
plan-2026-08-04T044628-4c240b4c/D-006 | 2026-08-05 | Use interleaved adjacent-pair packing (`np.repeat(..., 2, axis=-1)` on the angle table plus a pair-wise `(a, b) -> (-b, a)` rotation), and state at the code site that the split-half alternative is NOT interchangeable.
plan-2026-08-04T044628-4c240b4c/D-008 | 2026-08-05 | Implement it as a PRIVATE, UNREGISTERED `_SAM2RoPEAttention` inside `memory_attention.py` — full `build`/`get_config`/`compute_output_shape` treatment, but no `@keras.saving.register_keras_serializable()` and no public name.
plan-2026-08-04T044628-4c240b4c/D-009 | 2026-08-05 | Record the plan's named mutation as INERT and substitute an eager `float()` of a traced tensor (`float(ops.mean(features_pos))` in `SAM2MemoryAttention.call`) as G2.4's RED proof; note the measured trace behaviour at the guard's code site so steps 3 and 8 do ...
plan-2026-08-04T044628-4c240b4c/D-010 | 2026-08-05 | Keep the lag, put the whole schedule in ONE pure function (`hiera_block_specs`), and guard it twice: G3.1 asserts the exact per-block list, G3.2 asserts the delta-impulse receptive field of stage 2's first block.
plan-2026-08-04T044628-4c240b4c/D-012 | 2026-08-05 | Create `self.proj` unconditionally in `__init__` and BUILD it only when `dim != dim_out`.
plan-2026-08-04T044628-4c240b4c/D-013 | 2026-08-05 | Parameterize the neck by `num_pos_feats` in the REPO layer's units, default it to `d_model // 2`, expose the derived `pos_enc_channels` property, and assert the OUTPUT width (256) rather than the constructor argument. `f-6`'s "128 total channels" gloss is ...
plan-2026-08-04T044628-4c240b4c/D-014 | 2026-08-05 | Keep the drop-then-read order in `call()`, anchor it in-source with the two named wrong variants spelled out, and guard the WHICH question (G4.2, shape triples) separately from the HOW-MANY question (G4.1, list length).
plan-2026-08-04T044628-4c240b4c/D-016 | 2026-08-05 | DERIVE both the stage count (`log(total_stride)/log(stride)`) and the channel ladder (`× stride2` compounded from `mask_in_chans`) inside `__init__` and expose them as `num_layers` / `channel_sequence`; pad with an explicit `ZeroPadding2D` followed by a ...
plan-2026-08-04T044628-4c240b4c/D-018 | 2026-08-05 | Keep `t_rel == 1` as an explicit branch, anchor it in-source with the exact reason the "simplification" is invisible, and script G6.1's hand-derived table across BOTH strides AND both tracking directions rather than a single configuration.
plan-2026-08-04T044628-4c240b4c/D-019 | 2026-08-05 | Assign `t_pos = 0` and `tpos_slot = num_maskmem - 1` unconditionally to every conditioning frame, anchored in-source, and guard it at three temporal distances (20, 19 and 15 frames).
plan-2026-08-04T044628-4c240b4c/D-020 | 2026-08-05 | Append at the tail, anchor the reason in-source, and guard the POSITION by value (tail-slice equality plus a head-is-spatial assertion) rather than by the token count.
plan-2026-08-04T044628-4c240b4c/D-021 | 2026-08-05 | Store `token_offset = 1 if pred_obj_scores else 0` as an explicit attribute, derive every unpack slice from it, and guard the slice by VALUE against an identity-stub transformer rather than by shape.
plan-2026-08-04T044628-4c240b4c/D-022 | 2026-08-05 | Apply both lateral 1x1 convolutions INSIDE `predict_masks`; `call` accepts raw `transformer_dim`-wide high-resolution features. Refuse `high_res_features=None` when `use_high_res_features` is set, and refuse the converse.
plan-2026-08-04T044628-4c240b4c/D-023 | 2026-08-05 | Keep the two-condition guard, anchor the reason in-source, and guard all four boolean combinations by SHAPE plus one by VALUE.
plan-2026-08-04T044628-4c240b4c/D-024 | 2026-08-05 | `ops.argmax(..., axis=-1)` followed by `ops.take_along_axis(..., axis=1)`, anchored in-source, guarded at batch 2 with per-row argmax indices that DIFFER, plus a fixture-validity test asserting they differ.
plan-2026-08-04T044628-4c240b4c/D-026 | 2026-08-05 | Own the table in `SAM2.build`, have `SAM2MemoryBank.read` return `tpos_slots` + `frame_token_counts`, and expand them per TOKEN in `SAM2._temporal_embedding`; object-pointer tail tokens receive ZEROS.
plan-2026-08-04T044628-4c240b4c/D-027 | 2026-08-05 | Implement the fallback as a one-token memory of ZEROS with the learned `no_mem_pos_enc` (`(1, 1, mem_dim)`) as its position, keep `no_mem_embed` at `(1, 1, hidden_dim)` for the shipped directly-add path, and anchor the refusal to transcribe the reference ...
plan-2026-08-04T044628-4c240b4c/D-028 | 2026-08-05 | Do NOT run a dummy forward in `SAM2.build_from_config`. Run the static build chain only, and anchor the measurement plus the instrument that would detect a regression.
plan-2026-08-04T044628-4c240b4c/D-033 | 2026-08-05 | Apply `sigmoid(x) * scale + bias`; re-derive the guard over THREE candidates (reference, order-swapped, bare sigmoid) instead of two; anchor the order at the code site.
plan-2026-08-04T044628-4c240b4c/D-034 | 2026-08-05 | Accumulate both areas in float32 unconditionally, and add a `mixed_float16` arm at the SHIPPED 256x256 size with a negative control proving the size is in the overflowing regime.
plan-2026-08-04T044628-4c240b4c/D-035 | 2026-08-05 | Introduce the module constant `_HYPERNETWORK_MLP_DEPTH = 3` and use it for the hypernetworks; leave the IoU head reading `iou_head_depth`.
plan-2026-08-04T044628-4c240b4c/D-036 | 2026-08-05 | Build all four. Reuse SAM 1's `_build_mlp_head` for `obj_ptr_proj` rather than writing a third MLP builder; add `obj_ptr_tpos_proj` as a plain `Dense`; add a module-level `_sine_positional_encoding_1d`; add `no_obj_embed_spatial` as a fifth owned weight. Give ...
plan-2026-08-04T044628-4c240b4c/D-037 | 2026-08-05 | Default `soft_no_obj_ptr=False, fixed_no_obj_ptr=True`, and anchor at the signature that these are the CONFIG values rather than the reference class-signature values.
plan-2026-08-04T044628-4c240b4c/D-038 | 2026-08-05 | Add a module-level `_select_best_by_iou(tensor, iou)` returning a length-1 mask axis, and call it from BOTH sites.
plan-2026-08-04T044628-4c240b4c/D-039 | 2026-08-05 | Match the reference expression exactly: the multiply stays inside the `if`, the add moves outside.
plan-2026-08-04T044628-4c240b4c/D-040 | 2026-08-05 | Sine-encode `readout.obj_ptr_tpos` at `hidden_dim`, project it through `obj_ptr_tpos_proj` to `mem_dim`, and concatenate it as the pointer tail. Encode per TOKEN (the bank already repeat-interleaves), which is value-identical to the reference's ...
plan-2026-08-04T044628-4c240b4c/D-041 | 2026-08-05 | Add the parameter (default `True`, the shipped value) and the filter, direction-aware.
plan-2026-08-04T044628-4c240b4c/D-043 | 2026-08-05 | Add a module-level `NO_OBJ_SCORE = -1024.0` and a `SAM2._suppress_absent_object` applied in `_decode` immediately after the decoder call — so it precedes the gather, is what `_store_memory` reads, and is what the caller receives. Unconditional and un-flagged, ...
plan-2026-08-04T044628-4c240b4c/D-044 | 2026-08-05 | Port the guard — gather only when the pointer axis is longer than 1, otherwise take index 0. `_select_best_by_iou` itself is unchanged (at its other call site the two axes are the same tensor's).
plan-2026-08-04T044628-4c240b4c/D-045 | 2026-08-05 | KEEP the `variable_dtype` cast and re-derive the guard as a PRECISION measurement against a float64 oracle, with a dead-component twin (the same expression computed in the compute dtype) asserted to be genuinely worse FIRST. Rewrite the docstring's rationale ...
plan-2026-08-04T044628-4c240b4c/D-047 | 2026-08-05 | KEEP the behaviour, correct the record, and document it at `SAM2.call`. Also decided explicitly, and NEGATIVELY, about upstream's `torch.clamp(low_res_masks, -32.0, 32.0)`: it is NOT ported.
plan-2026-08-04T044628-4c240b4c/D-051 | 2026-08-05 | Fold the frame axis INTO the mask axis (`(B,T,h,w)` with `M == 1`), reusing `match_mask_axis` and both `sam_mask_loss.py` adapters unmodified, and refuse `multimask_output=True` with a message naming the frame/mask interleave.
plan-2026-08-04T044628-4c240b4c/D-052 | 2026-08-05 | Object-score supervision is stock `keras.losses.BinaryCrossentropy(from_logits=True)` — no new class. IoU gating is done by zeroing both channels of `iou_supervision` inside the model, reusing `SAMIoULoss` unchanged. One new class, `SAM2GatedMaskLoss`, ...
plan-2026-08-04T044628-4c240b4c/D-053 | 2026-08-05 | OUTCOME (a). It traces. Adopt it: `SAM2TrainingModel.call` constructs a FRESH `SAM2MemoryBank` as a local variable on every call, and the iteration-1 selection policy (`select_frames`, `select_object_pointer_frames`, the `t_pos` slots, the object-pointer ...
plan-2026-08-04T044628-4c240b4c/D-054 | 2026-08-05 | CALL `self.sam2._decode(...)` directly rather than re-implementing its tail; call `_select_best_by_iou`, `_temporal_embedding` and `_mark_occlusion` directly too. Only `_condition_on_memory` and `_store_memory` are duplicated, as `_condition` / `_store`, ...
plan-2026-08-04T044628-4c240b4c/D-057 | 2026-08-05 | Import `_box_from_mask` privately from `train.sam.data`, alongside the public `INSTANCE_RENDERERS`, `MASK_DIVISOR`, `MIN_MASK_PIXELS`, `RECORD_*`, `jitter_box`, `sample_point_in_mask` and `sample_point_outside_mask`. Do NOT copy it and do NOT publish it.
plan-2026-08-04T044628-4c240b4c/D-058 | 2026-08-05 | Emit only the two target keys the wrapper actually returns, sourced from that module's exported constants, and couple the pipeline to the model by a CANARY assertion: `set(targets) == set(training_model.OUTPUT_KEYS)`. Step 4 adds `SAM2_IOU_SUPERVISION` to ...
plan-2026-08-04T044628-4c240b4c/D-059 | 2026-08-05 | Set the bar's sweep speed to the tracked instance's own bounding-box WIDTH and its coverage margin to `speed * (occlusion_frames - 1) + 1`. Every in-window frame is then fully covered and every out-of-window frame is fully DISJOINT from the object -- no ...
plan-2026-08-04T044628-4c240b4c/D-068 | 2026-08-05 | `build_sam2_video_dataset` batches with `drop_remainder=True`, making the batch axis STATIC; the drop is counted and logged; and `SAM2TrainingConfig` REFUSES a split smaller than one batch (which would otherwise yield zero batches, i.e. an empty validation ...
plan-2026-08-04T044628-4c240b4c/D-069 | 2026-08-05 | Correct the DOCSTRING to spell the runnable form (`--smoke --num-frames 1 --occlusion-frames 0`) and state WHY the extra flag is mandatory. Do NOT auto-clamp `occlusion_frames` to the window.
plan-2026-08-04T044628-4c240b4c/D-072 | 2026-08-05 | Compute `SAM2GatedMaskLoss`'s focal term in LOGIT space, in the loss's own `call`, and move the focal gate from the probabilities to the per-pixel loss. Keep dice unchanged. Do NOT touch `segmentation_loss.py` or `sam_mask_loss.py`.
plan-2026-08-04T044628-4c240b4c/D-074 | 2026-08-05 | Truncated BPTT. KEEP the `ops.stop_gradient` on the memory encoder's INPUTS in `_store` (that is the truncation); DROP the read-side detach in `_condition`; construct the per-call `SAM2MemoryBank` with `stop_gradient=False`; ADD a `stop_gradient` on the ...
plan-2026-08-04T044628-4c240b4c/D-075 | 2026-08-05 | Add `open_object_score_gate(model)` to the TRAINER (`train/sam2/train_sam2.py`), called once on a freshly built model, writing upstream's own "assume present" constant `+10.0` into `pred_obj_score_head`'s final bias. NOT in `SAM2TrainingModel.build()`.
plan-2026-08-04T044628-4c240b4c/D-080 | 2026-08-05 | Give the image-cross sub-block ONLY its own bias-injection path, implemented as a module-private helper inside `src/dl_techniques/models/sam3/decoder.py` — a bespoke scaled-dot-product-attention that adds the bias to the raw scores before softmax. The ...
plan-2026-08-04T044628-4c240b4c/D-083 | 2026-08-05 | Add `scale_pos: float = 1.0` and multiply the `t_x`/`t_y` COORDINATES by it, on BOTH axes, inside `_build_angle_tables()` immediately before the frequency outer product; thread it through `get_config()`; guard `scale_pos > 0` at construction; gate the ...
plan-2026-08-04T044628-4c240b4c/D-085 | 2026-08-05 | Compose a module-PRIVATE, UNREGISTERED `_Sam3ViTDetAttention` inside `models/sam3/vitdet.py`. REJECT `create_attention_layer('multi_head', ...)` (contract: `layers/attention/factory.py`'s `multi_head` entry maps to `MultiHeadAttention`, whose ...
plan-2026-08-04T044628-4c240b4c/D-086 | 2026-08-05 | A module-private helper `_make_layer_scale(init_values, name)` returns a REAL `keras.layers.Identity` when `init_values is None`, and a `LearnableMultiplier(multiplier_type='CHANNEL', initializer=Constant(init_values), constraint=None)` otherwise. Both are ...
plan-2026-08-04T044628-4c240b4c/D-087 | 2026-08-05 | Do not implement padding. `Sam3ViTDetBlock.__init__` RAISES `ValueError` when `window_size > 0` and `window_size` does not divide both axes of `input_size`.
plan-2026-08-04T044628-4c240b4c/D-088 | 2026-08-05 | `self.mlp_hidden_dim = int(self.dim * self.mlp_ratio)`, with the discriminating probes placed at ratios whose product is NON-integral.
plan-2026-08-04T044628-4c240b4c/D-089 | 2026-08-05 | Derive `rope_scale_pos` from the grid attention ACTUALLY runs on — the window for a windowed block, the image grid for a global block — and pass it to step 1's `AxialRoPE2D(scale_pos=...)`. `use_interp_rope=False` forces 1.0 everywhere.
plan-2026-08-04T044628-4c240b4c/D-090 | 2026-08-05 | Implement literal `ops.tile(table, (1, g//side + 1, g//side + 1, 1))[:, :g, :g, :]`, channels-last, with the `side == grid` case short-circuiting to a plain reshape (upstream's `if size != h or size != w` guard).
plan-2026-08-04T044628-4c240b4c/D-091 | 2026-08-05 | `ln_pre` runs at that exact point. `ln_pre=False` / `ln_post=False` map to real, unconditionally-built `keras.layers.Identity` sub-layers rather than to a `call()`-time branch — the same rule as D-086.
plan-2026-08-04T044628-4c240b4c/D-092 | 2026-08-05 | (a) The trunk returns exactly ONE tensor — the last global block's output after `ln_post` — and short-circuits the loop there. (b) It returns it channels-LAST `(B, g, g, C)`, which is the repo-wide Keras convention, and the step-3 neck is written against ...
plan-2026-08-04T044628-4c240b4c/D-095 | 2026-08-05 | Both projection convs carry `use_bias=True` and there is NO normalization anywhere in `Sam3DualViTDetNeck` -- not between them, not after them.
plan-2026-08-04T044628-4c240b4c/D-096 | 2026-08-05 | A module-private `_encode_position(pe_layer, feature, d_model)` does the transpose and the cast, and then re-checks the RESULT's full spatial-plus-channel shape against the feature it must be added to. `d_model // 2` is passed to the layer's constructor. The ...
plan-2026-08-04T044628-4c240b4c/D-097 | 2026-08-05 | Two separate `_build_scale_stack` invocations, so no layer object and no variable is shared. The guards on this are a trainable-parameter COUNT and a weight-BINDING/independence probe -- never an output comparison.
plan-2026-08-04T044628-4c240b4c/D-098 | 2026-08-05 | Store both stacks FLAT (`List[Layer]`), keep the per-scale boundaries in `_branch_sizes`, and expose a `branches()` accessor that re-slices them.
plan-2026-08-04T044628-4c240b4c/D-101 | 2026-08-05 | Construct with `output_mode='none'` and never call `get_sequence_features()`. No pooling of any kind is reproduced -- there is no end-of-text argmax step in this wrapper, pooled or discarded.
plan-2026-08-04T044628-4c240b4c/D-102 | 2026-08-05 | Construct with `normalization_position='pre'` and all three dropout rates at 0.0, and assert the resulting structure in-suite rather than trusting the constructor call to stay put.
plan-2026-08-04T044628-4c240b4c/D-104 | 2026-08-05 | Adopt PADDING-IS-TRUE as this layer's contract, name the argument `prompt_padding_mask` so the polarity is in the name, and floor the pooling divisor at one.
plan-2026-08-04T044628-4c240b4c/D-105 | 2026-08-05 | Keep them different, anchor the reason at the clamp site, and pin the difference with a probe INSIDE `(10, 12]`.
plan-2026-08-04T044628-4c240b4c/D-106 | 2026-08-05 | REJECT both by CONTRACT and compose the MLP from plain `Dense`/`Dropout`/`LayerNormalization` sub-layers inside `model_misc.py`. No new public class.
plan-2026-08-04T044628-4c240b4c/D-111 | 2026-08-05 | Ship the clamp (the shipped constructor exposes `clamp_presence_logits` / `clamp_presence_logit_max_val` exactly as upstream does, both defaulting to upstream's `True` / `10.0`), and record here that at the pinned SHA the reference's clamp is a provable ...
plan-2026-08-04T044628-4c240b4c/D-112 | 2026-08-05 | Zero-initialize the kernel AND the bias of `bbox_embed`'s last `Dense`, anchor the reason at the construction site, and pin it with a three-test family: the weights are zero, layer 0 moves nothing, and a NON-zero head does move it.
plan-2026-08-04T044628-4c240b4c/D-113 | 2026-08-05 | Take the loss on a LATER LAYER'S HIDDEN STATE rather than on its reference box. The box head reaches `hidden_states[2]` ONLY through the reference chain, so the detach makes that gradient exactly `None`. Pre-checked with a cheap tape probe BEFORE the mutation ...
plan-2026-08-04T044628-4c240b4c/D-117 | 2026-08-05 | A-2's fallback taken: the mask-embedding MLP and the einsum are composed in-file from three `Dense` layers. `EomtMask` is REJECTED-by-contract at this site -- the ninth reuse candidate this plan has refuted on contract rather than on name (G-5).
plan-2026-08-04T044628-4c240b4c/D-118 | 2026-08-05 | Pass `epsilon` EXPLICITLY to every `GroupNormalization`, defaulting the class's own `norm_epsilon` to `1e-5`, and pin it in-suite against the framework default.
plan-2026-08-04T044628-4c240b4c/D-119 | 2026-08-05 | The head has NO presence mechanism at all -- no constructor flag, no sub-layer, no output key, no attribute. Not a disabled one: an absent one.
plan-2026-08-04T044628-4c240b4c/D-120 | 2026-08-05 | NEAREST upsample, ADDITIVE fusion, and an EXPLICIT width check on the fused tensor inside `_merge` rather than leaving the width to the following convolution's build.
plan-2026-08-04T044628-4c240b4c/D-121 | 2026-08-05 | Start at the coarsest and walk the remaining levels in reverse; anchor the reason at the loop and guard it with a RESOLUTION assertion, not a value one.
plan-2026-08-04T044628-4c240b4c/D-122 | 2026-08-05 | Ship the reference's expression verbatim -- probabilities multiplied, product re-logit'd, result clamped to `[-10, 10]` -- and record that the clamp is a provable NO-OP at the reference's own eps, with the discriminating literal being `eps`, not the clamp. ...
plan-2026-08-04T044628-4c240b4c/D-123 | 2026-08-05 | The cause is not serialization at all: the repository's shared `StochasticDepth` treats `training=None` as TRAINING. Add `drop_path_rate` to the variant table -- `0.1` for `sam3` (the reference's number) and `0.0` for `tiny` -- and pin the trap itself with ...
plan-2026-08-04T044628-4c240b4c/D-125 | 2026-08-05 | `Sam3Image` REQUIRES both, refuses construction otherwise with a named `ValueError`, and returns a FIXED five-key dict on every call.
plan-2026-08-04T044628-4c240b4c/D-134 | 2026-08-05 | CARRY D-042 FORWARD AND ACCEPT the divergence -- do not fix the computation. Three payments, all made here: (a) the deviation, its mechanism and its measured magnitude at all four SHIPPED grids are stated in `Sam3DualViTDetNeck`'s class docstring and at the ...
plan-2026-08-04T044628-4c240b4c/D-136 | 2026-08-05 | D-126's COUNT IS WRONG and is corrected here: TWO of the eight `build` methods lacked the guard, not one. Add `if self.built: return` to BOTH `Sam3DotProductScoring.build` and `Sam3TextEncoder.build`, and add an executed double-`build()` test class to each of ...
plan-2026-08-04T044628-4c240b4c/D-137 | 2026-08-05 | FIX one divergence, ACCEPT and RECORD three. (1) `presence_token` moves from `RandomNormal(stddev=1.0)` to `GlorotUniform` -- FIXED, and pinned by a distribution test with a RED arm. (2) The `sqrt(2)` q/k/v scale divergence (reference fuses `in_proj_weight` ...
plan-2026-08-04T044628-4c240b4c/D-142 | 2026-08-05 | ACCEPT AND PIN, as D-134 did for `pi/H` -- and record the WORKING REMEDY rather than claiming none exists. Four payments: (a) the divergence, its mechanism and its re-measured magnitude are stated in the module docstring, the class docstring and a qualified ...
plan-2026-08-05T124709-6c4fac48/D-006 | 2026-08-05 | Delete the padded-row unit-box substitution guard in the GIoU cost matrix; the guard was unreachable defensive code, pinned instead by nan-only-when-both-degenerate tests.
plan-2026-08-05T124709-6c4fac48/D-007 | 2026-08-05 | Implement the reference's sqrt(batch-size) video/image loss-scale correction but default it off, since single-stage image training has nothing to correct.
plan-2026-08-05T124709-6c4fac48/D-010 | 2026-08-05 | Give the packed target's meta row a real is_exhaustive channel so divisor #5's weak-loss path is a genuinely exercisable switch, not dead code.
plan-2026-08-05T124709-6c4fac48/D-011 | 2026-08-05 | Define a from-logits BCE/focal spelling with correct zero-logit gradients, deliberately using alpha_t rather than SAM 2's flat-alpha helper to match the reference.
plan-2026-08-05T124709-6c4fac48/D-012 | 2026-08-05 | Apply the presence gate multiplicatively to per-element BCE before reduction, not after — the two are numerically different and only the former matches the reference.
plan-2026-08-05T124709-6c4fac48/D-015 | 2026-08-05 | Give the packed target/prediction layout one home and one inverse (pack/unpack) inside the loss module rather than a second copy in the training model.
plan-2026-08-05T124709-6c4fac48/D-017 | 2026-08-05 | Derive a new `small` variant geometry from the reference's own ratios, since nothing exists between `tiny` and `sam3` and `sam3` cannot fit an AdamW state on a 12GB GPU.
plan-2026-08-05T124709-6c4fac48/D-018 | 2026-08-05 | Always pass `training` explicitly for the `sam3` variant, since this repo's shared `StochasticDepth` treats `training=None` as training and fakes a reinitialization defect.
plan-2026-08-05T124709-6c4fac48/D-019 | 2026-08-05 | Size the synthetic shape-category vocabulary to fit `tiny`'s vocab_size so the text vocabulary is never the binding constraint on a learnability claim.
plan-2026-08-05T124709-6c4fac48/D-023 | 2026-08-05 | Use `drop_remainder=True` on the synthetic data pipeline after measuring the dynamic-batch-axis `fit()` trap does not bite here.
plan-2026-08-05T124709-6c4fac48/D-027 | 2026-08-05 | Derive the trainer's optimizer recipe from the reference's own 100-image fine-tune config rather than inventing hyperparameters.
plan-2026-08-05T124709-6c4fac48/D-028 | 2026-08-05 | Ship a metrics harness reporting achieved box/mask IoU and presence accuracy, never loss alone, since a collapsed head can have a perfectly stable loss.
plan-2026-08-05T124709-6c4fac48/D-029 | 2026-08-05 | Freeze the image trunk (`backbone.trainable = False`) before compile for the frozen-vs-joint A/B, so the two arms differ in exactly one thing.
plan-2026-08-05T124709-6c4fac48/D-040 | 2026-08-06 | Tie `pad_n_queries` to the variant's own query count instead of a fixed 200, removing an unintended 6.25x classification-loss attenuation at `small`.
plan-2026-08-05T124709-6c4fac48/D-041 | 2026-08-06 | Select the best checkpoint by `val_box_iou` instead of `val_loss`, since `presence_loss` was 61.7% of val_loss while nearly image-independent.
plan-2026-08-05T124709-6c4fac48/D-047 | 2026-08-06 | Fix `num_boxes` to the conjunction `valid AND w>0 AND h>0`, restoring the validity flag a prior port-fidelity fix had silently dropped — a real shipped regression.
plan-2026-08-06T055747-1e650383/D-001 | 2026-08-06 | Probe first (pre-committed 3-arm gradient verdict), then ship deep supervision as repeated uniform blocks appended to the same packed prediction tensor (`[Q rows | presence row] x (1 + n_aux)`, main block first), rather than a new packed row axis or a second packed tensor.
plan-2026-08-06T055747-1e650383/D-004 | 2026-08-06 | Auxiliary decoder-layer blocks are summed into the total loss at the SAME weight as the main term — a plain sum, no per-layer coefficient or depth decay — because the reference applies none.
plan-2026-08-06T055747-1e650383/D-005 | 2026-08-06 | Auxiliary blocks' mask channels are zero-filled by the packer and the loss computes no mask term for any auxiliary block, matching the reference's `Masks` loss defaulting `compute_aux=False` while `Boxes`/`IABCEMdetr` default True.
plan-2026-08-06T055747-1e650383/D-006 | 2026-08-06 | `evaluate_sam3` branches on `model.num_aux_layers` and packs the auxiliary blocks too via `Sam3Image.call_per_layer`, so it never mis-slices the loss's packed-block-count contract while still performing exactly one forward pass per batch.
plan-2026-08-06T185813-fd80240f/D-005 | 2026-08-07 | Hard-code `anchor_size = 0.1776` — the seed-pooled mean of `sqrt(w*h)` over the train split's valid GT boxes — as `Sam3EncoderQuerySelection`'s default, kept distinct from `baselines.py`'s unrelated hand-written `GRID_BOX_SIZE = 0.2`.
plan-2026-08-06T185813-fd80240f/D-006 | 2026-08-07 | Selected proposal boxes enter the decoder detached (`stop_gradient`), and query content stays the learned `query_embed` table — the encoder head is supervised only by its own packed block, never by backprop through the decoder.
plan-2026-08-06T185813-fd80240f/D-007 | 2026-08-07 | The "which blocks get packed" rule lives in one module-level function, `select_prediction_blocks`, called by both `Sam3TrainingModel.call` and `evaluate_sam3`, so the evaluation path cannot silently drift from the training path again.
plan-2026-08-06T185813-fd80240f/D-009 | 2026-08-07 | Ship the connected-components detector as a first-class tested baseline member, excluded from `family_max` by an allowlist rather than a blocklist, so a pre-registered bar cannot be silently raised by a comparator that beats it.
plan-2026-08-07T065516-6add49a9/D-003 | 2026-08-07 | SUPPLEMENT the prompted-category filter with additive eval-only all-category record keys outside the training `signature`, never relax the filter, so every published `box_iou` stays comparable and zero RNG draws move.
plan-2026-08-07T065516-6add49a9/D-014 | 2026-08-07 | FiLM-modulate `memory` from the pooled prompt BEFORE both proposal MLPs (a bias on the objectness logits cannot change an argsort), created only under a default-OFF flag and deliberately NOT zero-initialized because this is a gate, not an anchor displacement.
plan-2026-08-07T065516-6add49a9/D-022 | 2026-08-07 | Fold the `--distractor-gap` and `--prompt-swap` results into the `--json` payload keyed per seed, writing into a COPY of the per-seed dict so `family_max`'s allowlist can never see them.
plan-2026-08-07T065516-6add49a9/D-023 | 2026-08-07 | Move the shared matched-IoU reduction and the distractor-target rule DOWN the dependency chain (`data.py` <- `train_sam3.py` <- `baselines.py`) rather than take a lazy import cycle or a third copy of the reduction.
plan-2026-08-07T174622-3b8002c3/D-001 | 2026-08-07 | Thread a defaulted val_seed_offset parameter through baselines.py only (never train_sam3.py::create_datasets), second offset 20_000, so a second independently-seeded val split needs zero published numbers to move.
plan-2026-08-07T174622-3b8002c3/D-003 | 2026-08-07 | Add one additive best_gap_model.keras ModelCheckpoint monitoring val_box_distractor_gap, proven training-neutral with its own control-first CPU A/B, leaving SELECTION_METRIC/EarlyStopping untouched.
plan-2026-08-10T130454-3649c19e/D-006 | 2026-08-10 | Delete the full DSv2 consumer closure (embedding_unet.py, lmunet.py, four trainers) rather than partially rewriting the two U-Nets to drop their downsampling paths.
plan-2026-08-10T130454-3649c19e/D-008 | 2026-08-10 | Keep the live, tested use_gate kwarg and its inert-sublayer pattern — D-007's premise (that the rewrite had already dropped it) measured false — and execute the DSv2 deletion D-005/D-006 actually required instead.
plan-2026-08-10T130454-3649c19e/D-010 | 2026-08-10 | Import the activation trio (_activation_spec/_resolve_activation/_serialize_activation) from clifford_block into clifford_rnn rather than re-copying a stale fork or extracting a new shared module.
plan-2026-08-10T130454-3649c19e/D-011 | 2026-08-10 | Delete the orphaned CliffordBottleneckMonitorCallback and its test rather than generalizing the surviving ConvUnextBottleneckMonitorCallback sibling to a model that never implements the contract it needs.
plan-2026-08-10T130454-3649c19e/D-012 | 2026-08-10 | Repoint test_weight_transfer.py's fixture to small functional Keras models sharing CliffordNetUNet's layer-naming convention, decoupling the utility's tests from any model package.
plan-2026-08-10T130454-3649c19e/D-014 | 2026-08-10 | Key memory_llm_custom_objects() by keras.saving.get_registered_name(cls), derived per class, rather than the bare class name or a hard-coded literal Custom> string.
plan-2026-08-10T130454-3649c19e/D-027 | 2026-08-10 | Scope the CliffordUNet architecture refusal to DenoiserPrior.from_pretrained's dynamic branch only, restoring the working fixed256 load that D-009 had wrongly refused outside any branch.
plan-2026-08-10T130454-3649c19e/D-029 | 2026-08-10 | Keep the hard private-Keras-import tripwire (no try/except fallback) for DropoutRNNCell, since a fallback cannot preserve the dropout-mask lifecycle it would silently lose.
plan-2026-08-10T183739-b007f435/D-003 | 2026-08-11 | Declare pad_token_id ADVISORY (never auto-derive attention_mask from it); document the caller-must-supply-mask contract and guard it with a masked-vs-unmasked forward-differs test.
plan-2026-08-10T183739-b007f435/D-006 | 2026-08-11 | Validate position_embedding_type against a hard-coded enum in BertEmbeddings.__init__ and raise ValueError, rather than a silent else-fallback like the normalization_type defect this plan removes.
plan-2026-08-10T183739-b007f435/D-007 | 2026-08-11 | Add the two new conditional sub-layer constructions IN PLACE, without reordering existing statements, to preserve the RNG-seeded weight-initialization order the I-1 bit-identity requirement depends on.
plan-2026-08-10T183739-b007f435/D-008 | 2026-08-11 | Compute the sinusoidal position table in float32 unconditionally, then cast to the dtype of the tensor it is actually summed with — fixes the mixed_float16 crash without a dtype-only fix that fails to survive the layer boundary.
plan-2026-08-10T183739-b007f435/D-009 | 2026-08-11 | Hoist normalization_type and position_embedding_type enums to module-level constants shared by the constructor and the factory validator, removing a hand-maintained lockstep duplication.
plan-2026-08-10T183739-b007f435/D-010 | 2026-08-11 | Move type_vocab_size to optional (default None) with a conditional-required rule in validate_embedding_config keyed off the registry's own default, not a hard-coded True.
plan-2026-08-10T183739-b007f435/D-011 | 2026-08-11 | Pass every kwarg explicitly at the create_embedding_layer call site, including four that look redundant, each proven load-bearing by mutation — factory defaults are BERT's, not neutral.
plan-2026-08-10T183739-b007f435/D-012 | 2026-08-11 | Document, do not fix, that from_variant(pretrained=<path>) raises on every route (unbuilt: wrong dummy-input dtype; built: unsupported by_name kwarg); keras.models.load_model(path) is the working route.
plan-2026-08-10T183739-b007f435/D-013 | 2026-08-11 | Assert STRUCTURE (mask_zero flags, traceback origin, built sub-layer class/epsilon, post-forward weight paths) rather than forward output where the shipped behaviour is numerically invisible, and treat in-test assertion ORDER as load-bearing.
plan-2026-08-10T183739-b007f435/D-015 | 2026-08-11 | Normalize position_ids rank once in BertEmbeddings.call for both branches (broadcast rank-1, raise on unsupported rank), removing the asymmetry where the sinusoidal branch crashed on inputs the learned branch accepted.
plan-2026-08-10T183739-b007f435/D-016 | 2026-08-11 | Compute the sinusoidal table in the wider of float32/variable_dtype (gated on variable_dtype, never compute_dtype) so a float64 policy gets float64-grade precision, not just float64 storage.
plan-2026-08-10T183739-b007f435/D-017 | 2026-08-11 | Anchor the sinusoidal/rank test file's value-varying assertions against the measured M28 vacuity (a shape+finiteness check that stayed green under a silent position clamp).
plan-2026-08-10T183739-b007f435/D-018 | 2026-08-11 | Correct the mask_zero rationale to the measured fact — it is inert downstream, no _keras_mask survives the embedding sum at either setting — after the reviewer showed D-011's original two-masks-collide hazard claim was false.
plan-2026-08-10T183739-b007f435/D-021 | 2026-08-11 | Materialize non-tensor position_ids via hasattr/convert_to_tensor before reading rank, fixing a regression D-015 itself introduced (list/tuple/int inputs raised an unnamed AttributeError on both branches).
plan-2026-08-11T012340-f63796dc/D-001 | 2026-08-11 | Build one new standalone BeitAttention layer registered as a 'beit' attention type, compose BeitModel from TransformerLayer with no signature change, and mirror models/energy_transformer/ file-for-file for the backbone/MIM/classifier split.
plan-2026-08-11T012340-f63796dc/D-003 | 2026-08-11 | Adopt timm's layer_scale_init_value split (0.1 for tiny/small/base, 1e-5 for large) and ship tiny/small as repo-convention invented variants, both facts stated in the README.
plan-2026-08-11T012340-f63796dc/D-006 | 2026-08-11 | Generalize validate_attention_config's positive-value loop to compare a sequence value componentwise (a scalar wrapped in a 1-tuple), keyed on the parameter name exactly as before, so a tuple window_size validates at every door.
plan-2026-08-11T012340-f63796dc/D-007 | 2026-08-11 | BeitModel.final_norm is created and applied only when use_mean_pooling is False; at the default (True) each head owns its own normalization instead of the trunk norming twice.
plan-2026-08-11T012340-f63796dc/D-008 | 2026-08-11 | src/train/beit/common.py re-exports build_raw_image_dataset/build_optimizer/SUPPORTED_DATASETS/DATASET_NUM_CLASSES from train.energy_transformer.common rather than forking the pipeline, and authors only load_frozen_tokenizer.
plan-2026-08-11T012340-f63796dc/D-011 | 2026-08-11 | Keep BeitAttention's relative-position index as a numpy int32 array computed in build() and convert it to a tensor inside call(), so fit() on an unbuilt model no longer raises InaccessibleTensorError.
plan-2026-08-11T012340-f63796dc/D-012 | 2026-08-11 | Pin the MIM head's cls-slice by elementwise identity rather than by shape, and mark the site with a qualified anchor stating what not to do (do not drop the last token, do not rely on shape alone).
plan-2026-08-11T110821-54118fdd/D-001 | 2026-08-11 | Add an explicit input_mode: Literal["image","sequence"] constructor kwarg; sequence mode accepts both rank-3 and rank-4 by expanding/squeezing at the top/end of call() so the internal 4-D weight layout is unchanged; causal=True implies input_mode="sequence"; the three external call sites are not migrated.
plan-2026-08-11T110821-54118fdd/D-002 | 2026-08-11 | Resolve the DEFAULT ctx_norm to zero_centered_rms_norm for causal and non-causal sequence mode (image mode keeps batch_norm), and raise ValueError naming the leak when a caller explicitly selects batch_norm, bias_free_batch_norm, or global_response_norm under causal=True.
plan-2026-08-11T110821-54118fdd/D-005 | 2026-08-11 | Compute _causal_cumulative_mean's cumsum and divisors in float32 (widened only from float16/bfloat16) then cast back to the input dtype, keeping keras.ops.shape-based dynamic sequence length graph-safe; delete the dead _causal_seq_len state.
plan-2026-08-11T110821-54118fdd/D-006 | 2026-08-11 | Add a channel-axis-only contract-named raise to SparseRollingGeometricProduct.build() (never a rank check), since the class is measured rank-agnostic at ranks 2-5 with shipped rank-2 consumers (clifford_rnn.py, clip projection heads).
plan-2026-08-11T110821-54118fdd/D-008 | 2026-08-11 | Split mixed-precision test coverage by strength: smoke guards for finite/dtype/shape at block level (which do not discriminate the D-005 fix), plus one isolated-reduction test asserting relative error against a float64 reference tolerance set to the worst value across devices, not the headline number.
plan-2026-08-11T110821-54118fdd/D-009 | 2026-08-11 | Prove the two already-correct characterization claims (wedge/ctx_mode inertness, activations-are-applied) exclusively by dead-component/behaviour-swap injection rather than by revert, using a shared twin-builder that verifies elementwise weight equality before any output comparison, with layer_scale_init=1.0 (never the layer's own 1e-5 default) to avoid a vacuous float32 comparison.
plan-2026-08-11T110821-54118fdd/D-011 | 2026-08-11 | Delete and git rm --cache the tracked cliffordnet.pdf and rewrite the stale .gitignore prose, adding no *.pdf or path-scoped ignore rule so a rebuilt PDF stays visible to git status; this deliberately reverses part of same-day commit 3efebe97b on explicit, later, policy-consistent instruction.
plan-2026-08-11T110821-54118fdd/D-012 | 2026-08-11 | Add CliffordNetBlock.from_config to repair legacy causal configs that pair causal=True with a now-rejected normalization_type, substituting the safe norm and warning that the reloaded model is numerically different from the saved one; the constructor raise itself is left unchanged for fresh code.
plan-2026-08-11T110821-54118fdd/D-013 | 2026-08-11 | Re-check the input_mode="sequence" singleton axis-1 constraint on every call (not just build()) using the static shape with an is-not-None short-circuit, rejecting a traced-tensor check because AutoGraph silently converts a data-dependent Python if into a tf.cond instead of raising.
plan-2026-08-11T141925-eb34352d/D-003 | 2026-08-11 | Migrate the fourth caller-side rank-4 wrap found in _TextLMWrapper.call (src/train/cliffordnet/train_clip.py, outside the brief's three-file scope) too, since leaving a known copy of exactly the just-deleted construct would re-create the duplication this migration exists to remove.
plan-2026-08-11T165740-53dac34a/D-004 | 2026-08-11 | Migrate all five newly-found hand-rolled linear drop-path schedules (dino_v1, dino_v2, dino_v3, convnext_v1, convnext_v2) as completion-fix step 8.1 rather than deferring them, since their omission from the declared 9-site scope was an oversight, not a deliberate exclusion, and the real total is 14 sites.
plan-2026-08-11T201945-91938f65/D-001 | 2026-08-11 | Wire FNet's use_stochastic_depth/stochastic_depth_rate knob by adding two independent StochasticDepth instances to FNetEncoderBlock (one per residual branch) and forwarding a linear_drop_path_rates schedule from the model, imitating transformer.py's majority two-instance pattern.
plan-2026-08-11T201945-91938f65/D-002 | 2026-08-11 | Restore the canonical residual + drop-path wiring inline at all three ConvUNextModel call sites, set the blocks' own dropout_rate to 0.0, land this change last after fastvlm and fnet, and re-verify the no-checkpoint fact immediately before landing.
plan-2026-08-11T201945-91938f65/D-003 | 2026-08-11 | Add use_stochastic_depth/stochastic_depth_rate to AttentionBlockVLM and forward them into its internal TransformerLayer, then drive FastVLM's stage-3 schedule through stochastic_depth_rate while reverting dropout_rate to self.dropout_rate, de-collapsing the prior max(...) merge.
plan-2026-08-11T201945-91938f65/D-004 | 2026-08-11 | Write the residual + drop-path pattern inline at each convunext site, imitating bfconvunext.py rather than importing from it or creating a new shared helper, keeping the new-abstractions budget at 0/2.
plan-2026-08-12T123743-e798a9e1/D-006 | 2026-08-12 | Move build_raw_image_dataset (+ _normalization_constants, _augment, 5 module constants, 3 foreign in-function anchors) from energy_transformer/common.py into train/common/datasets.py character-for-character, replacing the old body with a re-export; update beit/common.py's D-008 anchor text to name the new home.
plan-2026-08-12T123743-e798a9e1/D-007 | 2026-08-12 | Move build_optimizer (a trainer-config adapter, not optimizer-construction logic) into new src/train/common/optimizer.py, replacing the old body with a re-export; add DINO's missing shared-symbol identity pin; leave EnergyTraceCallback unpromoted (single consumer).
plan-2026-08-12T123743-e798a9e1/D-008 | 2026-08-12 | Canonical create_clm_loss_fn keeps the two logger.info provenance lines and ASCII "gamma" spelling (3-of-4 majority across the four CLM copies), pinned by tests RED-proven by deleting each log line.
plan-2026-08-12T123743-e798a9e1/D-009 | 2026-08-12 | Canonical load_train_val_datasets keeps data_seed as a required parameter with no default (cliffordnet's =42 default was dead) and keeps the ValueError's two-legal-values message suffix (3-of-4 majority).
plan-2026-08-12T123743-e798a9e1/D-010 | 2026-08-12 | Every migrated CLM trainer keeps a module-level alias block binding each old private name to the canonical train.common.clm_pretrain object (plain assignment, never a wrapper def) so is-identity holds through re-export.
plan-2026-08-12T123743-e798a9e1/D-011 | 2026-08-12 | Repoint wave_field/train_memory.py's _extract_step_from_checkpoint import at train.common.clm_pretrain directly (no more reaching it through a sibling trainer) while keeping the alias in wave_field/pretrain.py so the old cross-module import path still resolves.
plan-2026-08-12T123743-e798a9e1/D-014 | 2026-08-12 | Move resolved_output_dir into train.common.args.resolved_run_dir and adopt at all 3 SAM sites via an import alias, anchoring the parents[3] repo-root-depth coincidence at the moved function so a future relocation of args.py cannot silently produce a wrong root.
plan-2026-08-12T123743-e798a9e1/D-015 | 2026-08-12 | Extract the SAM argv-to-config merge core into train.common.args.config_values_from_args (4 params, smaller than the plan's declared 5) with CLI_TO_CONFIG/SMOKE_PRESET/build_parser staying local per-model data; each SAM trainer's own D-041 anchor stays at its call site with a pointer to the new home.
plan-2026-08-12T123743-e798a9e1/D-021 | 2026-08-12 | Give the bert/fnet best-checkpoint path exactly one producer (train/common/callbacks.py's best_checkpoint_path(results_dir)) shared by the ModelCheckpoint writer and the post-training-analysis reader; results_dir becomes a required parameter with no config.save_dir fallback; fixes the live F-23 crash and bert/deploy.py's third, differently-spelled read site. Supersedes D-017.
plan-2026-08-12T201216-50fc0975/D-006 | 2026-08-13 | Import setup_gpu inside main() (function-scope, immediately after parse_arguments) in all four fixed entry points rather than fixing the repo-wide import-time GPU allocation in train/common/image_text.py, which is out of Files To Modify and left as a recorded, unfixed follow-up. [SUPERSEDED by plan-2026-08-13T045759-fde437ba/D-003 — the import-time allocation is now FIXED, so all four deferred imports are harmless rather than load-bearing; the anchors and their four in-code comments are kept for the history.]
plan-2026-08-13T045759-fde437ba/D-003 | 2026-08-13 | Convert train/common/image_text.py's IMAGE_MEAN/IMAGE_STD from module-scope tf.constant(...) to plain Python lists, killing the import-time GPU allocation paid by every importer of train.common, rather than a lazy accessor or an inline-into-augment_and_normalize; guarded by a subprocess import-allocation test with a positive liveness arm plus a real tf.data .map()-path bit-identity test, both RED-proven.
plan-2026-08-13T091555-230c101d/D-005 | 2026-08-13 | Add `WaveFieldLLM._download_weights` as a `@staticmethod` that always raises `NotImplementedError`, and route `from_variant`'s `pretrained=True`-with-no-path branch through it inside a NARROW `except (IOError, OSError, ValueError)`, never `except Exception`.
plan-2026-08-13T091555-230c101d/D-006 | 2026-08-13 | SPLIT the adoption: ADOPT the positional table + add via `create_embedding_layer('positional_learned', ..., dropout_rate=0.0, scale=self.initializer_range)`; REFUSE folding `embed_dropout` into it — it stays a separate layer applied after `embed_norm`.
plan-2026-08-13T091555-230c101d/D-008 | 2026-08-13 | Document causality as a MEASURED property of the exact `(field_size, max_seq_len)` pair, never a guarantee, pinned by a parametrized sweep whose assertion DIRECTION is per-ratio (clean `< bound`, leaky `> bound`); perturb every position, not only the last; add no constructor raise.
plan-2026-08-13T091555-230c101d/D-009 | 2026-08-13 | Treat the first-try-passing untied-kernel identity assertion as a probe defect; re-derive it at a square (`vocab_size == embed_dim`) config where the alias defect is shape-legal, keeping only the falsifiable parts at the rectangular config.
plan-2026-08-13T091555-230c101d/D-010 | 2026-08-13 | ADOPT `ClmPretrainConfig` (43 fields, gpt2's declaration order) in `src/train/common/clm_pretrain.py`, re-exported from `train/common/__init__.py`; both `TrainingConfig`s inherit it, overriding only `save_dir` (+ `field_size` for wave_field).
plan-2026-08-13T091555-230c101d/D-011 | 2026-08-13 | ACCEPT the `field_size` field-order move (dataclass inheritance appends subclass-only fields last); REFUSE `train_memory.py` for the base config — its file has 0 hits for `num_layers`/`num_heads`, so inheriting would add dead fields.
plan-2026-08-13T091555-230c101d/D-012 | 2026-08-13 | Convert `FinetuneConfig` to a `@dataclass` (measured: `vars()` on the plain class wrote 0 of 29 fields) and keep the `save_config_json`/`save_training_history_json` adoption at all four F-06 sites.
plan-2026-08-13T091555-230c101d/D-017 | 2026-08-13 | Raise `CLEAN_BOUND` from 1e-6 to 1e-5 (measured: 1e-6 sits ~8 float32 ulps inside process-history-dependent fp noise on GPU) so the causality pin is a strict partition (clean `< 1e-5 <` leaky) rather than a flaky exact-zero claim.
plan-2026-08-13T183738-24486492/D-001 | 2026-08-14 | Build the faithful MobileCLIP2 port as a NEW sibling package `models/mobile_clip_v2/` plus a NEW layer subpackage `layers/fastvit/`, leaving v1's semantics untouched, and reuse the existing `MobileClipTextEncoder` as the text tower via a cross-package import.
plan-2026-08-13T183738-24486492/D-002 | 2026-08-14 | Extend `MobileOneBlock` ADDITIVELY as plan step 1 — first, on a green tree — with every new kwarg defaulting to today's behaviour, pinned by a defaults-unchanged VALUE-identity test against a transcribed pre-change oracle plus a `tests/test_models/test_fastvlm/` regression gate in the same commit.
plan-2026-08-13T183738-24486492/D-004 | 2026-08-14 | Pre-register all five known reference divergences as X-1..X-5 and require them in BOTH decisions.md and the package README's Deviations section; the mci0/mci1/mci2 variant tables carry NO local oracle (timm not installed).
plan-2026-08-13T183738-24486492/D-007 | 2026-08-14 | Add two additive `MobileOneBlock` kwargs, `norm_epsilon` (default 1e-3, unchanged) and `padding_mode` (default 'keras_same', unchanged), with reference values (1e-5 epsilon, symmetric padding) passed uniformly from every `layers/fastvit/` and `models/mobile_clip_v2/` site via a new `layers/fastvit/reference.py`, fixing two adversarial-review CRITICALs without moving `models/fastvlm/`'s numerics.
plan-2026-08-14T042537-ff96c6c6/D-002 | 2026-08-14 | Harden ONLY `create_embedding_layer` to raise on unknown kwargs, by porting `ffn/factory.py`'s inline predicate and marker constant; the other five silent-drop factories are recorded as follow-up, not fixed here, and no shared strict-factory helper is created.
plan-2026-08-14T135600-mcsplit/D-001 | 2026-08-14 | SPLIT `models/mobile_clip_v2/` in two: the assembled MCi tower becomes its own standalone model package `models/fastvit/` (`FastVitImageEncoder` + `MCI_VARIANTS`, class names UNCHANGED so every `Custom>` registry key survives), and the CLIP model moves to `models/mobile_clip/mobile_clip_v2.py` beside v1. `layers/fastvit/` does NOT move — the 8 block primitives stay in `layers/` under the factory-first reuse policy. AMENDS plan-2026-08-13T183738-24486492/D-001: the `MobileClipTextEncoder` reuse is now an IN-PACKAGE import (`from .components import ...`), not a cross-package one; the two-adapter-site causal-mask invariant that forbids copying it is unchanged.
plan-2026-08-14T135600-mcsplit/D-002 | 2026-08-14 | Populate `models/mobile_clip/__init__.py` with a curated `__all__` exporting both models' classes and factories, but DELIBERATELY NOT `MODEL_VARIANTS`: v1's is the class attribute `MobileClipModel.MODEL_VARIANTS` (`b`/`s0`/`s1`/`s2`) and v2's is module-level (`mobileclip2_s*`), with disjoint keys and with `mci0`/`mci1`/`mci2` meaning real MCi rows in v2 and `keras.applications` stand-ins in v1. A package-level re-export would make that collision invisible.
plan-2026-08-14T135600-mcsplit/D-003 | 2026-08-14 | Merge the two READMEs rather than keeping a v1-only and a v2-only document: `models/mobile_clip/README.md` covers both generations with an explicit v1-vs-v2 table and a §17 "which should I use", and `models/fastvit/README.md` is new and owns the MCi tower + deviations X-1/X-2/X-3/X-5 in full (mobile_clip summarizes and links). Also REPLACED v1's `faghri2023mobileclip` BibTeX entry, whose author list, title, venue and year were all fabricated, with the real Vasu et al. CVPR 2024 citation.
plan-2026-08-14T160000-mcv2shape/D-001 | 2026-08-14 | Give `MobileClipV2Model` v1's API SHAPE: `MODEL_VARIANTS` moves to a nested CLASS attribute (`embed_dim` + `image_config` + `text_config`, sub-dicts being literal encoder kwargs), `__init__` collapses 18 flat kwargs to v1's `(embed_dim, image_config, text_config, logit_scale_init, output_dict, ...)`, and `output_dict` + `summary()` are adopted. Variant KEY NAMES stay (`mobileclip2_s0`...) — they are provenance-bearing, one row per supplied JSON config. AMENDS plan-2026-08-14T135600-mcsplit/D-002, whose rationale (v1 class-attribute vs v2 module-level) this retires: both tables are now class attributes, so the collision it guarded against is gone and the remaining hazards are the NAMING ones (`text_config['embed_dim']` is the text width; `image_config['variant']` is not the row key).
plan-2026-08-14T160000-mcv2shape/D-002 | 2026-08-14 | KEEP the nine v2 behaviours v1 lacks, against the pull to adopt v1's method list wholesale: tower injection + explicit tower serialization, per-tower `built` guards in `build()`, `get_build_config`/`build_from_config`, `compute_output_shape`, `compute_logit_scale`, `_validate_config`, `logit_scale_max`, the tower-name constants, and `logit_scale` created in `build()` not `__init__`. Each prevents a specific measured failure. ADD read-only `@property` accessors over the sub-dicts (surface v1 lacks) so validation, `summary()` and ~15 assertions do not become hand-written two-level dict lookups.
plan-2026-08-14T160000-mcv2shape/D-003 | 2026-08-14 | `output_dict=False` returns a 5-TUPLE `(image_features, text_features, logits_per_image, logits_per_text, logit_scale)`, NOT v1's 3-tuple, which would silently drop both logits matrices; `compute_output_shape` branches on the same flag. Replace the `len(params) > 10` anti-vacuity guard with an EXACT parameter-name set pin (strictly stronger: it also catches an ADDED parameter), and make the `_REFERENCE_VARIANTS` key-set check RECURSE into the sub-dicts — a flat `set()` over nested rows passes while an extra key hides one level down. Both proven RED.
plan-2026-08-14T092357-0e3d792d/D-009 | 2026-08-14 | Promote `_downsample_and_skip` to a serializable `DownsampleAndSkip` Layer with ONE name (the pooling name); the removed `pyramid_name` argument disappears rather than being preserved for symmetry.
plan-2026-08-14T092357-0e3d792d/D-010 | 2026-08-14 | The bf `ConvUNextStem` SURVIVES and is relocated into `models/convunext/model.py`, gaining `use_bias` + `stem_normalization`; its `package="dl_techniques.bias_free_denoisers"` decorator string is kept VERBATIM despite the module move, and the convunext-local twin is deleted.
plan-2026-08-14T092357-0e3d792d/D-011 | 2026-08-14 | Forward the ~40-parameter wrapper via `forwarded = dict(locals())` as the first body statement rather than a hand-listed keyword forward, so an omitted parameter fails loudly instead of silently dropping.
plan-2026-08-14T092357-0e3d792d/D-012 | 2026-08-14 | Ship exactly three bias-free guardrails (`final_activation`, `gabor_activation` when gabor-stem is on, `supervision_norm_center`) as one public allowlist `POSITIVELY_HOMOGENEOUS_ACTIVATIONS` owned by `models/convunext/model.py`, with `train/bfunet/common.py`'s `GABOR_ACTIVATIONS` now DERIVED from it instead of re-spelled.
plan-2026-08-14T092357-0e3d792d/D-013 | 2026-08-14 | `include_top=False` does NOT construct the final projection (the deleted subclass's weight-compat contract is unreproducible in a functional graph, measured by probe); the strided-conv downsample branch is channel-preserving with a raw-input skip.
plan-2026-08-14T092357-0e3d792d/D-014 | 2026-08-14 | `kwargs.setdefault('block_normalization', 'batchnorm')` lives ONLY in `bfconvunext.create_convunext_variant`, never in the shared variant dict, so the bare `create_convunext_denoiser` byte-identity tripwire and the bias-ON variants are untouched.
plan-2026-07-30T140922-8af1028f/D-022 | 2026-07-30 | Adopt `assemble_ffn_config` at all three with DISCARD semantics — the pre-filter drops `activation` for those four types and no rename is added — and record in each file that a consumer who genuinely wants the `differential` rename must route the site through `build_transformer_ffn_config` rather than hand-add a fou...
plan-2026-07-30T140922-8af1028f/D-037 | 2026-07-30 | Fix the CALLERS, per the user's standing ruling for this item ("fix the callers, then make it strict") and exactly as steps 3/4/6 did at 8 other sites. Route each model's self-built dict through `assemble_ffn_config(self.ffn_type, {...})` — i.e. declare it WRAPPER-config, which is what it is — at all SIX sites (`ffn...
plan-2026-07-31T210633-b63a35aa/D-003 | 2026-08-01 | Reword the warning to a compute/performance note and fix the stale test prose. **Do NOT add a `window_size` raise.** Back the reword with an executable guard: a test asserting the two WARNED geometries build with `declared == actual`.
plan-2026-07-31T210633-b63a35aa/D-004 | 2026-08-01 | Take (c) documentation + (b1) H-15 + (b3) H-16 + (b2) H-17. Reject (a) full consolidation. For H-16, raise a clear `ValueError` rather than auto-falling-back to a two-stage or `symmetric` pad. For H-15, keep `input_resolution` as an ACCEPTED-but-advisory constructor key rather than deleting it. Duplicate the `pad >=...
plan-2026-08-01T105809-dc0c402e/D-009 | 2026-08-01 | Record the falsification now and **carry it forward as a binding constraint on Step 8**, rather than transitioning to REFLECT at Step 2. `DINOTrainingModel.call()` must return a **SINGLE tensor** under stock `fit()` — the concrete candidates measured available are (a) concatenating student and teacher logits along t...
plan-2026-08-01T105809-dc0c402e/D-010 | 2026-08-01 | Pass `attention_args={"use_bias": self.qkv_bias}` **unconditionally**, converging v1 on `dino_v3.py:284`'s spelling (v3 never had this bug). **Deciding measurements** (`probe_step3_dino_v1.py`, keras 3.8.0, GPU 1): (a) `ATTENTION_REGISTRY['multi_head']['optional_params']` contains `use_bias` (default `False`) and NO...
plan-2026-08-01T105809-dc0c402e/D-011 | 2026-08-01 | **IMPLEMENT**, as a `keras.constraints.UnitNorm(axis=0)` on the final `Dense` kernel PLUS a one-off application of that same constraint at the end of `build()`. **Deciding measurement** (`probe_step3_dino_v1.py` § D): a `Dense(use_bias=False, kernel_constraint=UnitNorm(axis=0))` reaches per-output-column norms of `1...
plan-2026-08-01T105809-dc0c402e/D-012 | 2026-08-01 | **RAISE `NotImplementedError`**, with a message naming the exact missing capability and the two modules that would have to provide it. **Deciding measurement** (`probe_step3_dino_v1.py` § C, signature inspection on keras 3.8.0): `TransformerLayer.call` takes `(inputs, attention_mask, layer_idx, training)`; `MultiHea...
plan-2026-08-01T105809-dc0c402e/D-014 | 2026-08-01 | **Leave the single shared `StochasticDepth` instance as-is**, and pin the reason in BOTH the `DINOv2Block` class docstring and an in-code anchor at the creation site, naming the rejected alternative (`drop_path1`/`drop_path2`) explicitly. **Deciding measurement** (`scratchpad/probe_step4.py` § D, keras 3.8.0 / TF 2....
plan-2026-08-01T105809-dc0c402e/D-015 | 2026-08-01 | **Route A.** Under `positional_embedding_type='rope'` the encoder blocks are built with `attention_type='group_query'`, `num_kv_heads == num_heads`, and `rope_theta` / `rope_percentage` / `max_seq_len` in `attention_args`; the learned absolute positional-embedding table is **not created at all** (`self.pos_embed is ...
plan-2026-08-01T105809-dc0c402e/D-017 | 2026-08-01 | **`None` is the sentinel meaning "the caller said nothing" and defers to the variant's own `MODEL_VARIANTS` entry (or the version's default when the entry says nothing); an EXPLICIT non-`None` value ALWAYS wins.** Applied to five parameters: `create_dino_v{1,2,3}(patch_size=None)`, `create_dino_v2(ffn_type=None)`, `...
plan-2026-08-01T105809-dc0c402e/D-020 | 2026-08-01 | **FIX, in `DINOHead.call`**: run the pre-projection L2 normalization in `self.variable_dtype` (cast up, normalize, cast back to `compute_dtype`) instead of in `compute_dtype`. **Deciding measurement** (`scratchpad/probe_step7_fp16_hazards.py`, `probe_step7_head_zero.py`, keras 3.8.0 / TF 2.18.0, GPU 1): `keras.utils...
plan-2026-08-01T105809-dc0c402e/D-033 | 2026-08-01 | **FIX**, by converging `DINOv1.from_variant` on the merge form `config = cls.MODEL_VARIANTS[variant].copy(); config.update(kwargs)` + `**config`, i.e. an explicit architecture override in `**kwargs` WINS over the variant table. **Deciding measurement, which relocated the defect**: the collision is NOT in `create_din...
plan-2026-08-01T105809-dc0c402e/D-034 | 2026-08-01 | **Copy the student into the teacher UNCONDITIONALLY, at BOTH construction seams**, through ONE shared helper `sync_teacher_to_student` in `models/dino/common.py` (the module that already holds the other piece more than one DINO module needs verbatim). The two seams are `create_dino_teacher_student_pair` — a public e...
plan-2026-08-03T191222-1d751f81/D-005 | 2026-08-03 | Ship both the explicit `ValueError` and a `resize_longest_side` module-level function in a new `src/dl_techniques/models/sam/preprocessing.py`, marking step 8 `[RISK: medium]` and charging one of the two abstraction slots.
plan-2026-08-03T191222-1d751f81/D-007 | 2026-08-03 | Reshape `rel_pos` `(L, C)` to a 4-D BATCHED image `(1, 1, L, C)` and resize the width axis alone with `size=(1, max_rel_dist)`, then reshape back to `(max_rel_dist, C)` — L maps to width, C to channels.
plan-2026-08-03T191222-1d751f81/D-009 | 2026-08-03 | Add a serialized `attention_downsample_rate: int = 2` to both classes and keep TWO key dims per class — `self.key_dim` (full width, `self_attn` only) and `self.cross_attn_key_dim = embedding_dim // (num_heads * rate)` (the three cross-attentions) — plus a construction-time `ValueError` when `embedding_dim % (num_hea...
plan-2026-08-03T191222-1d751f81/D-010 | 2026-08-03 | Extract one shared `_build_mlp_head(...)` builder and drive **both** head families from `iou_head_depth` (validated positive at construction), flip the `activation` default to `'relu'`, and keep the last layer of every head linear. The `Dense` name template (`"hyper_dense{n}_{i}"`, `"iou_dense{n}"`) is chosen so tha...
plan-2026-08-03T191222-1d751f81/D-011 | 2026-08-03 | Ship the serialized `binarize_masks: bool = True` flag exactly as D-002 specified — default byte-identical, `False` returns the differentiable full-resolution float logits — plus the training-contract prose in the `SAM` class docstring, `call`'s `Returns:`, and a new README subsection, and 6 guards pinning the exact...
plan-2026-08-03T191222-1d751f81/D-013 | 2026-08-03 | Insert a single `point_embedding = ops.where(ops.expand_dims(labels, -1) == -1, ops.zeros_like(point_embedding), point_embedding)` immediately after the `forward_with_coords` call and BEFORE the three additive `ops.where` blocks, anchored with an explicit do-NOT note.
plan-2026-08-03T191222-1d751f81/D-014 | 2026-08-03 | Flip the `use_rel_pos` default to `True` and raise a `ValueError` at construction when `window_size > 0` **and** `global_attn_indexes` is empty, naming the offending `window_size`, the block count, and reference SAM's `(2, 5, 8, 11)` pattern; update the 9 pre-existing fixtures to pass a reference-shaped `global_attn...
plan-2026-08-03T191222-1d751f81/D-015 | 2026-08-04 | Refuse any `sparse_batch` that is neither `1` nor exactly `batch_size`, with a message naming both values, read from STATIC shapes so the check is a plain Python branch that is skipped (not traced) on an unknown batch dim.
plan-2026-08-03T191222-1d751f81/D-016 | 2026-08-04 | Check the mask's static spatial size against `4 * image_embedding_size` **inside `_embed_masks`**, i.e. at the point of violation, raising a `ValueError` that names the required size, the offending size, the grid it was measured against, and the corrective shape.
plan-2026-08-03T191222-1d751f81/D-018 | 2026-08-04 | **KEEP the dummy forward.** The pre-committed rule's *keep* branch fired: the `self.build(None)` chain alone materializes only **138 of 202** weights at load time. Documented the measured cost in the `build_from_config` docstring and pinned the property with three guards (`TestBuildFromConfigLoadCost`), so the "opti...
plan-2026-08-03T191222-1d751f81/D-019 | 2026-08-04 | Delete the class-level pair anyway, and record that the deletion rests on a different ground than F-13 gave: not "it is dead" but "it silently intercepts the assignment". After deletion `getattr` raises `AttributeError`, the short-circuit's `except` swallows it, and the instance attribute is written normally.
plan-2026-08-03T191222-1d751f81/D-024 | 2026-08-04 | Split the single knob in two: `activation: str = 'gelu'` (applied to `output_upscaling` only) and `mlp_activation: str = 'relu'` (applied to every non-final layer of the hypernetwork MLPs and the IoU head). Both are serialized in `get_config()` and both round-trip. Replace the certifying guard with `test_activation_...
plan-2026-08-03T191222-1d751f81/D-025 | 2026-08-04 | **REVERT the cache.** `_coord_grid` recomputes on every call, exactly as the pre-step-11 code did; the `_coord_grid_cache` / `_coord_grid_key` slots are gone from `__init__`. The extracted `_coord_grid` *method* is kept (it is a readability extraction, not the optimization). Add four graph-safety guards that execute...
plan-2026-08-03T191222-1d751f81/D-026 | 2026-08-04 | (a) Widen the guard's conjunct to `0 < window_size < grid_size`, keeping BOTH bounds. (b) Replace README § 15 wholesale with figures MEASURED from this package, plus a reproduction snippet, plus three tests that pin them. (c) Add a real `vit_b` forward at reference geometry. (d) Re-sort `decisions.md` into numeric o...
plan-2026-08-03T191222-1d751f81/D-035 | 2026-08-04 | Ship `SAMTrainingModel` per D-028 — submodule-only forward, dict output `{low_res_logits, iou_predictions}`, `build()` → `self.sam.build(None)`, `SeedGenerator` in `__init__`, explicit `get_config`/`from_config` — and close the moved-variable residual by measurement rather than by argument.
plan-2026-08-03T191222-1d751f81/D-036 | 2026-08-04 | Ship `SAMMaskLoss` (2-channel one-hot for focal, `(B*M,h,w,1)` for dice, raw layout refused by `_require_channels_last`) and `SAMIoULoss` (MSE over a **packed** `(B, M, 2)` tensor), plus a new **`gt_mask`-gated `iou_supervision` output key** on `SAMTrainingModel` carrying `[predicted_iou, stop_gradient(achieved_iou)...
plan-2026-08-03T191222-1d751f81/D-037 | 2026-08-04 | Ship `num_refinement_rounds` on `SAMTrainingModel` with the loop in `call()`; image encoder called ONCE outside the loop; `_feedback_mask` (stop-gradient, mask 0 only) and `_sample_error_points` as named seams; rounds concatenated round-major on the mask axis; **class default 1 (refinement OFF), trainer value `TRAIN...
plan-2026-08-03T191222-1d751f81/D-044 | 2026-08-04 | Make the derivation exist in exactly ONE place. `_match_mask_axis` is promoted to the public `match_mask_axis` (exported from `losses/__init__`), its docstring is extended with the interface contract and a do-NOT note, and `training_model.call` imports and calls it instead of recomputing anything. The dependency dir...

*`[PRE-MANIFEST]` block appended 2026-08-14 by `plan-2026-08-14T183218-f4c612aa` step 8, implementing ruling `plan-2026-08-14T163016-b7395570`/D-009. Each line names an anchor id referenced under `src/dl_techniques/models/` that resolves to NOTHING: absent from this manifest, its plan directory gone from `plans/`, and no `## <plan-id>` section in `plans/DECISIONS.md`. A stub carries no rationale — that is the whole point: a reader who greps such an id now gets "known-lost" rather than silence, and silence and known-lost are different answers. Stubs are NOT decision records; do not read one as evidence a choice was made here.*
*Re-derive the set (repo root): `grep -rhoE '[Pp]lan[_-][0-9]{4}-[0-9]{2}-[0-9]{2}[A-Za-z_0-9-]*/D-[0-9]+' src/dl_techniques/models/ | sort -u`, then subtract (a) ids already matching `^[A-Za-z0-9_.-]+/D-[0-9]+` in this file, (b) ids whose `plans/<plan-id>/` directory still exists, (c) ids with a `## <plan-id>` section in `plans/DECISIONS.md`. That pipeline measured 104 ids at append time; 95 of them are also flagged `[anchor-unknown-plan]` by `validate-plan.mjs`, the other 9 being references that are not literal `# DECISION` comments in a scanned source file.*

plan_2026-05-07_1519e34f/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-07_1519e34f/D-005 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-09_0f39a086/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-09_0f39a086/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-10_44694bc9/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-10_bd098beb/D-004 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-10_bdb2c84d/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-10_e6309bd5/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-11_0090b0b8/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-11_0a5779e8/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-11_3c3ed037/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-11_9357982a/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-11_a9e8e6f6/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-12_5f0e087c/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-12_f2d29729/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-12_f2d29729/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-12_f2d29729/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-15_fe237831/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-18_6776f8ba/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-23_0b664700/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-23_15151c75/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-23_15151c75/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-23_692fd5e5/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-24_aebd4cbb/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-24_ca745a6c/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-24_ca745a6c/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-24_ca745a6c/D-005 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-05-31_76981d58/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-03_943569ad/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-04_6196678d/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-04_6196678d/D-005 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-04_6196678d/D-007 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-04_6196678d/D-009 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-04_7ff8ea8b/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-04_d4ef81f1/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-04_d4ef81f1/D-004 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-05_56b39171/D-004 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-09_be55db55/D-004 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-10_7036cab1/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-11_f662207d/D-005 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-11_f662207d/D-007 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-11_f662207d/D-008 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-11_f662207d/D-009 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-11_f662207d/D-010 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-11_fe7401f4/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-12_59a18a10/D-005 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-12_59a18a10/D-007 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-12_dfce0712/D-005 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-12_dfce0712/D-006 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-12_dfce0712/D-007 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-12_dfce0712/D-008 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-12_dfce0712/D-009 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-13_ae9ee2cd/D-004 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-14_8c7365d0/D-006 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_00924f53/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_00924f53/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_2a23a001/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_2a23a001/D-004 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_39a31d4a/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_39a31d4a/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_39a31d4a/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_e2759fbc/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_e2759fbc/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_e2759fbc/D-004 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_e2759fbc/D-005 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_e2759fbc/D-006 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_e2759fbc/D-007 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_e2759fbc/D-008 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_e2759fbc/D-009 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_e6a0391c/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_e6a0391c/D-004 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-15_e6a0391c/D-008 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-16_535b4f02/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-16_5a05afc3/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-16_6e8c78a3/D-009 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-16_6e8c78a3/D-011 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-16_c8f3e9ca/D-004 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-16_c8f3e9ca/D-005 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-18_e1411ebf/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-19_ed071c02/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-20_0433c2f2/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-21_eb7fd829/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-21_eb7fd829/D-005 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-21_eb7fd829/D-006 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-26_0ec1a304/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-06-26_90d8cbe6/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-07-01_8054f023/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-07-04_58ac8e73/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-07-06_b17c1f83/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-07-10_be906be8/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-07-10_be906be8/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-07-11_bb4b38b5/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan_2026-07-11_bb4b38b5/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan-2026-07-14T163315-29a4fef4/D-009 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan-2026-07-14T163315-29a4fef4/D-010 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan-2026-07-14T163315-29a4fef4/D-011 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan-2026-07-15T015824-3c2159eb/D-002 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan-2026-07-15T015824-3c2159eb/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan-2026-07-15T015824-3c2159eb/D-004 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan-2026-07-15T053724-78001af1/D-003 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan-2026-07-15T114613-5add9baa/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan-2026-07-15T114613-5add9baa/D-004 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan-2026-07-15T140843-168d5bac/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
plan-2026-07-15T144225-5b25d9f1/D-001 | [PRE-MANIFEST] | Rationale not recorded: this anchor predates the manifest and its plan directory no longer exists, so no rationale survives to copy here. The payload is the in-source `DECISION` comment itself — `grep -rn` this id under `src/dl_techniques/models/`; the manifest was only ever the index. Stub appended by plan-2026-08-14T183218-f4c612aa step 8.
