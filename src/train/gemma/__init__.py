"""Pattern-3 (subword CLM) trainer for the Gemma3 model package.

See ``train_gemma.py`` for the entry point and ``common.py`` for the
config/pipeline it drives. ``Gemma3`` is the package's only architecture, so
there is no D-002-style version-disambiguation decision to record here.
"""
