"""Training assets for ``dl_techniques.models.SAM.SAM3``.

This package is deliberately empty of re-exports: import from the submodules
directly (``from train.sam3.data import build_sam3_dataset``), matching
``train/sam/`` and ``train/sam2/``.
"""
