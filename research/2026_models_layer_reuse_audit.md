# Audit: Inline Layers in models/ vs Reusable layers/
*Date: 2026-06-13. Scope: classify every inline `keras.layers.Layer` subclass in `src/dl_techniques/models/` against the reusable vocabulary in `src/dl_techniques/layers/`. Report-only — no code changed.*

## Scope & Method
This audit reads every inline `keras.layers.Layer` subclass defined inside `src/dl_techniques/models/` — approximately **94 classes across 53 files / ~40 packages**, per `findings/audit-surface.md`. The `model-import-scan.md` Cluster 6 probe additionally surfaces a `PatchEmbedding` in `sam/image_encoder.py` that is **not** in the 94-line inventory, bringing the working total to **~95**; the exact denominator is reconciled in the Summary (Synthesis step).

This is a **report-only** deliverable: nothing under `src/dl_techniques/models/` or `src/dl_techniques/layers/` is modified. The user reviews the report and decides what (if anything) to act on later.

The reference vocabulary is `src/dl_techniques/layers/`, catalogued in `findings/layers-inventory.md`. It exposes 8 factory registries:
- **attention** — 23 keys
- **ffn** — 15 keys
- **norms** — 16 keys
- **embedding** — 10 keys
- **activations** — 22 keys
- **mixtures** — 3 keys
- **seq-pooling** — 3 keys
- **heads** — 21 keys

plus canonical standalone primitives (`RMSNorm`, `TransformerLayer`, `SwiGLUFFN`, `SqueezeExcitation`, `PatchEmbedding2D`, `StochasticDepth`, `LearnableMultiplier`, `Sampling`, `VectorQuantizer`, geometric `CliffordNetBlock` / `GatedGeometricResidual`, etc.).

## Rubric
Each inline class receives exactly one of three verdicts:

- **REPLACE** — an existing `layers/` class or factory key is functionally equivalent. The row names the exact target (class + import path, or factory key) and flags **drop-in** vs **needs-adapter**.
- **RELOCATE** — reusable, but no `layers/` equivalent exists; it belongs there. The row names the proposed `layers/` home and states why it is reusable.
- **KEEP** — model-specific; stays inline. The row justifies why.

### Source-read rules (anti-false-positive)
Distilled from `findings/sample-classification.md` § Rubric Gotchas. Every REPLACE/RELOCATE claim must satisfy these:

1. **Read BOTH sides' `call` / `build` / `get_config` before any REPLACE or RELOCATE — never name-match.** Check bias, normalization `center`, output format, and loss contract on both the inline class and the candidate.
2. **One-param gaps still count as REPLACE-with-adapter, not KEEP.** A single discriminating parameter (e.g. `use_bias` on a gate `Dense`) is an adapter, not a reason to keep a duplicate class.
3. **Output-contract differences are silent-semantics traps and block a clean REPLACE.** Bare logits vs a dict-with-softmax (e.g. `from_logits=True` vs probabilities) changes loss semantics even when the underlying math is identical.
4. **Same-name-different-math is NOT a replacement.** `_LearnedQueryPool1D` (scaled dot-product single-query) is not `SequencePooling('attention')` (tanh-projected context vector); identical names, fundamentally different operations.
5. **Flag latent bugs found during the audit, but do NOT recommend preserving them.** A relocation/replacement is an opportunity to fix a bug, never to carry it forward.

## Summary

95 inline `keras.layers.Layer` subclasses were audited across `src/dl_techniques/models/` (94 from the `audit-surface.md` inventory + 1 SAM `PatchEmbedding` surfaced by the `model-import-scan.md` Cluster 6 probe): **11 REPLACE** (delete inline, import from `layers/`), **25 RELOCATE** (move to `layers/`, no current equivalent), **59 KEEP** (model-specific or decision-locked).

### Counts by verdict

**Overall:**

| Verdict | Count | % of 95 |
|---|---|---|
| REPLACE | 11 | 11.6% |
| RELOCATE | 25 | 26.3% |
| KEEP | 59 | 62.1% |
| **Total** | **95** | **100%** |

**Per-batch breakdown:**

| Batch | Total | REPLACE | RELOCATE | KEEP |
|---|---|---|---|---|
| Batch 0 — bias_free_denoisers + cliffordnet | 14 | 5 | 6 | 3 |
| Batch A — transformer/LLM family | 21 | 2 | 2 | 17 |
| Batch B — vision/conv family | 15 | 0 | 6 | 9 |
| Batch C — VLM/multimodal/generative | 12 | 0 | 4 | 8 |
| Batch D — SSL/JEPA/encoder-decoder/segmentation | 13 | 3 | 3 | 7 |
| Batch E — super-res/world-model/memory/misc | 20 | 1 | 4 | 15 |
| **Grand total** | **95** | **11** | **25** | **59** |

### REPLACE list

Sorted drop-in first (highest value, lowest risk), then needs-adapter. All 11 rows.

**Drop-in replacements (delete inline class, import from `layers/` with matching args):**

| Class | file:line | Target (`layers/` class or factory key) | Drop-in? | Adapter note |
|---|---|---|---|---|
| `_LayerScale1D` | `cliffordnet/clip.py:153` | `layers/layer_scale.py:LearnableMultiplier` | drop-in | Pass `multiplier_type='CHANNEL'`, `initializer=Constant(1e-5)`, `constraint=None`; checkpoint shim needed for existing `Custom>_LayerScale1D` saves (default `constraint='non_neg'` differs). |
| `DINOv2Block` | `dino/dino_v2.py:68` | `layers/transformers/transformer.py:TransformerLayer` | drop-in | Config: `normalization_position='pre'`, `use_layer_scale=True`, `layer_scale_init_value=init_values`, `use_stochastic_depth=True`, `stochastic_depth_rate=stochastic_depth_rate`; all factory params already delegated identically. |
| `ByteTokenizer` | `modern_bert/components.py:10` | `layers/blt_blocks.py:ByteTokenizer` | drop-in | Unify on `errors='replace'` (candidate uses `errors='ignore'` on encode); both stateless, `vocab_size=260`, pad/bos/eos/unk IDs identical. |
| `TRMReasoningModule` | `tiny_recursive_model/components.py:47` | `layers/reasoning/hrm_reasoning_module.py:HierarchicalReasoningModule` | drop-in | Bit-for-bit equivalent; identical parameter names and injection mechanism. Add `from layers.reasoning.hrm_reasoning_module import HierarchicalReasoningModule` to `tiny_recursive_model/components.py`. |

**Needs-adapter replacements (minor `layers/` extension required before deletion):**

| Class | file:line | Target (`layers/` class or factory key) | Drop-in? | Adapter note |
|---|---|---|---|---|
| `_ClassificationHeadBlock` | `cliffordnet/unet.py:83` | `layers/heads/vision/factory.py:ClassificationHead` | needs-adapter | Candidate applies softmax + returns dict; inline returns bare logits. Add `use_logits=True` mode (or strip softmax in caller) — output-contract trap (rule 3). |
| `BiasFreeClifordNetBlock` | `cliffordnet/denoiser.py:61` | `layers/geometric/clifford_block.py:CliffordNetBlock` | needs-adapter | Candidate `center=True` default; add `norm_center: bool = True` param to `CliffordNetBlock`. Candidate already accepts `use_bias`. |
| `BiasFreeGatedGeometricResidual` | `cliffordnet/denoiser.py:246` | `layers/geometric/clifford_block.py:GatedGeometricResidual` | needs-adapter | Candidate gate `Dense(C, use_bias=True)`; inline uses `use_bias=False`. Add `use_bias: bool = True` to `GatedGeometricResidual` — one-param gap (rule 2). |
| `BiasFreeConditionedGGR` | `cliffordnet/conditional_denoiser.py:78` | `layers/geometric/clifford_block.py:GatedGeometricResidual` | needs-adapter | Byte-for-byte duplicate of `BiasFreeGatedGeometricResidual`. Same one-param gap; both become dead code once `GatedGeometricResidual` gains `use_bias`. |
| `HashNGramEmbedding` (blt_hrm.py) | `modern_bert/modern_bert_blt_hrm.py:191` | `layers/embedding/hash_ngram_embedding.py:HashNGramEmbedding` (post-relocation of components.py version) | needs-adapter | Intra-package duplicate with Python loop bug and AVERAGE vs SUM aggregation. Delete after `components.py:94` version is promoted to `layers/`; update callers to import from `layers/`. |
| `CausalSelfAttnMLPBlock` | `video_jepa/predictor.py:63` | `layers/transformers/transformer.py:TransformerLayer` | needs-adapter | Identical pre-norm + LayerScale(1e-5) structure; uses raw `keras.layers.MultiHeadAttention` instead of factory. Adapter: switch to `create_attention_layer('multi_head', ...)` and verify causal-mask forwarding through factory before merging. |
| `PatchEmbedding` [+scope] | `sam/image_encoder.py:117` | `layers/embedding/patch_embedding.py:PatchEmbedding2D` | needs-adapter | Candidate always flattens to `(B,N,D)`; SAM needs 4D `(B,H/p,W/p,D)` for window attention. Add `flatten: bool = True` param to `PatchEmbedding2D`; when `False`, skip reshape at line 247. |

### RELOCATE list

Grouped by proposed destination subpackage. All 25 rows.

**`layers/geometric/clifford_block.py` — Clifford geometric primitives (3 classes):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `BiasFreeConditionedCliffordBlock` | `cliffordnet/conditional_denoiser.py:187` | `layers/geometric/clifford_block.py` as **ConditionedCliffordNetBlock** | `CliffordNetBlock` arch + `use_bias=False`/`center=False` + two optional FiLM conditioning arms (`cond_gamma_proj` multiplicative, `class_proj` additive); conditioning flags absent from `CliffordNetBlock`. General to any conditional Clifford encoder. Fix additive bug (appendix #1) on relocate. |
| `BiasFreeGeometricDownsample` | `cliffordnet/conditional_denoiser.py:460` | `layers/geometric/clifford_block.py` as **BiasFreeGeometricDownsample** | Novel geometric strided-downsample (ctx: DWConv→BN→SiLU; detail: Dense→AvgPool; fusion: SparseRollingGeometricProduct→Conv2D→BN); no geometric-aware downsample in `layers/`. General to any Clifford encoder. |
| `BiasFreeGeometricUpsample` | `cliffordnet/conditional_denoiser.py:628` | `layers/geometric/clifford_block.py` as **BiasFreeGeometricUpsample** | Symmetric upsample via `UpSampling2D(2,'nearest')` + optional channel transition + Clifford refinement; no geometric-aware upsample in `layers/`. Belongs alongside `BiasFreeGeometricDownsample`. |

**`layers/` top-level — conditioning/modulation (2 classes):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `DenseConditioningInjection` | `bias_free_denoisers/bfunet_conditional_unified.py:112` | `layers/` (or `layers/ffn/`) as **BiasFreeFeatureModulation** | Bias-free two-input FiLM (multiplicative/concatenation only; additive rejected). Cross-cutting for bias-free U-Net / VAE / diffusion models; no FiLM/conditioning-injection layer exists in `layers/`. |
| `DiscreteConditioningInjection` | `bias_free_denoisers/bfunet_conditional_unified.py:247` | `layers/` as **BiasFreeEmbeddingBroadcast** | Embedding → spatial broadcast (`spatial_broadcast` / `channel_concat`). Standard in conditional diffusion / generative U-Nets. Fix in-`call` sublayer construction bug (appendix #2) on relocate. |

**`layers/sequence_pooling/` — attention pooling variant (1 class):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `_LearnedQueryPool1D` | `cliffordnet/clip.py:198` | `layers/sequence_pooling/` as **LearnedQueryPool** | Learnable query scaled dot-product pool `(B,D)` with optional mask; NOT `SequencePooling('attention')` (different math). Reusable for CLIP/EVA-style class-token-free attention pooling. Preserve `mask` arg on relocate. |

**`layers/embedding/` — new embedding primitives (3 classes):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `HashNGramEmbedding` (components.py) | `modern_bert/components.py:94` | `layers/embedding/hash_ngram_embedding.py` as **HashNGramEmbedding**; register factory key `hash_ngram` | Canonical vectorized byte-ngram embedding (idiomatic sublayers, graph-mode safe, SUM aggregation). General byte-level NLP primitive; promote this version, delete `blt_hrm.py:191` duplicate. |
| `PositionalEncoding` | `tree_transformer/components.py:32` | `layers/embedding/positional_encoding_sine_1d.py` as **PositionalEncodingSine1D**; register factory key `sine_1d` | Fixed Vaswani sinusoidal PE `(1,max_len,d)` non-trainable; not covered by `positional_learned`, `continuous_sincos`, `scalar_sinusoidal`, or `PositionEmbeddingSine2D`. Reusable by any non-RoPE transformer. |
| `PositionEmbeddingRandom` | `sam/prompt_encoder.py:88` | `layers/embedding/positional_embedding_random.py` as **PositionEmbeddingRandom** | Non-trainable Gaussian random matrix for Fourier spatial PE `call(size)→(2*D,H,W)`; distinct from `random_fourier_features.py` (data-space RBF) and `PositionEmbeddingSine2D` (deterministic). Reusable for SAM, NeRF, conditional diffusion with spatial prompts. |

**`layers/masking/` — new subpackage: SSL masking primitives (2 classes):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `TubeMaskGenerator` | `video_jepa/masking.py:38` | `layers/masking/tube_mask_generator.py` as **TubeMaskGenerator** | Stateless argsort-of-noise spatial tube mask `(B,H_p,W_p)`; no weights. Cross-SSL: same argsort idiom as `masked_autoencoder.patch_masking`. Reusable for MAE, V-JEPA, SimMIM, I-JEPA. |
| `PatchMasking` | `masked_autoencoder/patch_masking.py:12` | `layers/masking/patch_masking.py` as **PatchMasking** | Pixel-space patch masking `(B,H,W,C)` with learnable/noise/zero/constant fill; argsort sampling. Cross-cutting SSL primitive (MAE/SimMIM/BEiT/MaskFeat). Co-locate with `TubeMaskGenerator`. |

**`layers/vae/` — new subpackage: VAE primitives (4 classes):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `_L2ConditionalPrior` | `convnext_patch_vae/model_hierarchical.py:50` | `layers/vae/` as **HierarchicalConditionalPrior** | Top-down conditional prior (UpSampling→Conv2D→ConvNextV2Block→LN→mu+log_var heads); Ladder-VAE / VDVAE family primitive. Document resolution-agnostic (D-001) and zero-init (D-002) constraints on relocate. |
| `_CoarseLatentHead` | `convnext_patch_vae/model_hierarchical.py:293` | `layers/vae/` as **PoolDerivedGaussianHead** | Lightweight 3-op pool-derived Gaussian head (AvgPool→mu_head+log_var_head Conv2D). Reusable for any hierarchical VAE deriving coarse latent from fine features. Document D-001 constraint. |
| `ResnetBlock` | `ideogram4/vae.py:76` | `layers/vae/` as **GroupNormResnetBlock** | Flux2 KL-VAE GroupNorm(32) residual block (GN→swish→Conv2D→GN→swish→Conv2D + optional 1x1 shortcut). 2 current callers (ideogram4 + sd3_mmdit). No GroupNorm-based ResNet block in `layers/`; document channels-must-be-divisible-by-32 constraint. |
| `AttnBlock` | `ideogram4/vae.py:191` | `layers/vae/` as **VAEAttnBlock** | Flux2 KL-VAE spatial self-attention (GN→1x1 Conv Q/K/V→flatten→scaled-dot-product→reshape→proj_out→residual). 2 current callers (ideogram4 + sd3_mmdit). No GroupNorm+Conv Q/K/V spatial attention in `layers/`. Fix `ops.stack` reshape bug (appendix #8) on relocate. |

**`layers/transformers/` — transformer spatial wrapper (1 class):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `AttentionBlockVLM` | `fastvlm/components.py:15` | `layers/transformers/` as **SpatialTransformerBlock** | Flatten `(B,H,W,C)→(B,H*W,C)` → `TransformerLayer` → unflatten → optional `LearnableMultiplier`; zero original math, both sub-components already canonical. Standard VLM vision-backbone pattern; applicable to any 4D→sequence→4D flow. |

**`layers/heads/vlm/` — VLM projection head (1 class):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `ImageProjectionHead` | `mobile_clip/components.py:19` | `layers/heads/vlm/` as **ImageProjectionHead** | GAP → Dropout → Dense → optional activation; standard CLIP/ALIGN/BLIP image-to-embedding projector. Natural home alongside existing VLM head family. |

**`layers/fire_module.py` — SqueezeNet primitives (2 classes):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `FireModule` | `squeezenet/squeezenet_v1.py:79` | `layers/fire_module.py` as **FireModule** | Squeeze 1x1 → parallel expand 1x1+3x3 → Concatenate. Canonical SqueezeNet block; distinct from `SqueezeExcitation`. Reusable for any efficient conv model. |
| `SimplifiedFireModule` | `squeezenet/squeezenet_v2.py:77` | `layers/fire_module.py` as **SimplifiedFireModule** (alongside `FireModule`) | 3x3-only expand variant (no parallel 1x1); medical-imaging SqueezeNet simplification. Co-locate with `FireModule` in same file. |

**`layers/simple_gate.py` — parameter-free gating (1 class):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `SimpleGate` | `darkir/model.py:92` | `layers/simple_gate.py` as **SimpleGate** | Parameter-free channel-split gate `x1,x2=split(x,2,-1); return x1*x2`; operates on 4D NHWC. Distinct from Dense-based GLU/GeGLU/SwiGLU in `layers/ffn/`. Reusable for NAFNet/DarkIR-style efficient CNNs. |

**`layers/fft_layers.py` or `layers/frequency_mlp.py` — frequency-domain primitive (1 class):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `FreMLP` | `darkir/model.py:191` | `layers/fft_layers.py` (alongside FFTLayer/IFFTLayer) or `layers/frequency_mlp.py` as **FreMLP** | rfft2 → magnitude+phase → Conv2D MLP → reconstruct → irfft2; shape-preserving. Distinct from `FFTLayer`/`IFFTLayer` (full-spectrum, no inverse). Reusable for image restoration/enhancement. Fix symbolic-shape bug (appendix #6) on relocate. |

**`layers/sr_residual_block.py` / `layers/rdn_dense_block.py` — SR primitives (3 classes):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `EDSRResidualBlock` | `thera/edsr_backbone.py:57` | `layers/sr_residual_block.py` as **EDSRResidualBlock** | Conv-residual with `res_scale` float; no BN (EDSR design). Distinct from `layers/standard_blocks.py:BasicBlock` (BN+ReLU). Reusable for EDSR/THERA/any SR backbone. Document `res_scale=1.0` default matches THERA, `res_scale=0.1` recovers textbook EDSR. |
| `RDBConv` | `thera/rdn_backbone.py:77` | `layers/rdn_dense_block.py` as **RDBConv** (alongside `RDB`) | Atomic dense-growth conv: `relu(conv(x))` concatenated onto x, `C_in → C_in + growth_rate`. Reusable for any RDN-family SR model. |
| `RDB` | `thera/rdn_backbone.py:166` | `layers/rdn_dense_block.py` as **RDB** (alongside `RDBConv`) | Residual Dense Block: N×RDBConv → 1x1 local_fusion → residual add. Standard RDN/DenseNet-SR primitive. Co-locate with `RDBConv`. |

**`layers/ffn/normed_mlp.py` — normed MLP projector (1 class):**

| Class | file:line | Proposed `layers/` home | Why reusable |
|---|---|---|---|
| `MLPProjector` | `lewm/projector.py:26` | `layers/ffn/normed_mlp.py` as **NormedMLPProjector**; register factory key `normed_mlp` | Dense(hidden) → [LayerNorm] → GELU → Dense(output); mid-MLP LayerNorm absent from all 15 `layers/ffn/` factory keys. Standard I-JEPA/V-JEPA/LeWM SSL projector pattern. |

### KEEP rationale rollup

59 classes remain inline. By reason category:

| Reason category | Count | Example classes |
|---|---|---|
| (a) Bespoke composite / model-specific topology | 28 | `Qwen3NextBlock`, `Gemma3TransformerBlock`, `MMDiTBlock`, `DarkIREncoderBlock`, `NAMCell` |
| (b) RNN cell / stateful (exempt) | 0 | — |
| (c) Decision-locked (D-005, D-007, D-008) | 5 | `ideogram4/vae.py Downsample`, `ideogram4/vae.py Upsample`, `TheraHypernetwork`, `TheraTailPro`, `ConvNeXtPatchEncoder` (D-001/D-003) |
| (d) Thin glue / wrapper (inline-able) | 7 | `_DetectionHeadBlock`, `TheraTailAir`, `_Projection`, `ConvUNextStem` (B), `DilatedBranch`, `TRMInner`, `VideoJEPACliffordEncoder` |
| (e) Full model-level composite (pipeline, not primitive) | 19 | `Qwen3EmbeddingLayer`, `ReasoningByteCore`, `EDSRBackbone`, `RDNBackbone`, `ideogram4 Encoder/Decoder`, `TheraTailPlus`, `ARPredictor` |

**Sub-component reuse opportunities within KEEP classes** — the following KEEP rows have Notes flagging inline primitives that could additionally use `layers/` factories (low-risk incremental cleanup, not audit-blocking):

- `Mamba2ResidualBlock` (`mamba/components_v2.py:324`): `LayerNormalization(rms_scaling=True)` could be `layers/norms/rms_norm.py:RMSNorm`.
- `MambaResidualBlock` (`mamba/components.py:554`): `LayerNormalization` could be `create_normalization_layer('layer_norm')`.
- `WaveFieldDecoderBlock` (`wave_field_llm/wave_field_llm.py:67`): inline FFN `Dense(4D,gelu)→Dense(D)` could be `create_ffn_layer('mlp', hidden_dim=4*D, output_dim=D)`.
- `ConvUNextStem` (`convunext/model.py:39`): `LayerNormalization(epsilon=1e-6)` could be `create_normalization_layer('layer_norm', epsilon=1e-6)`.
- `TreeTransformerBlock` (`tree_transformer/components.py:504`): already uses `create_ffn_layer` and `create_normalization_layer` — no gap remaining.

## Appendix: Flagged bugs & anti-patterns

Bugs found during audit. None of these should be carried forward into any relocation or replacement. Severity codes: **correctness** = wrong output today; **serialization** = `from_config` / checkpoint round-trip broken; **XLA-compat** = fails under JAX/XLA trace; **footgun** = silent wrong behavior under specific invocation; **portability** = breaks non-TF backends.

1. **`cliffordnet/conditional_denoiser.py:391`** — `BiasFreeConditionedCliffordBlock.call`: additive discrete-conditioning injection (`x_norm = x_norm + cls_feat`) despite docstring claiming all modulations are multiplicative. Violates the bias-free property of the network. **Severity: correctness.** Fix on relocate to `ConditionedCliffordNetBlock`.

2. **`bias_free_denoisers/bfunet_conditional_unified.py:247`** — `DiscreteConditioningInjection.call`: constructs `RepeatVector`/`Reshape` Keras layer objects inside `call()` (new Python objects every forward pass) instead of `__init__`/`build`. Causes unbounded layer registry growth and is an anti-pattern that can break graph tracing. **Severity: footgun.** Fix on relocate to `BiasFreeEmbeddingBroadcast`.

3. **`modern_bert/modern_bert_blt_hrm.py:191`** — `HashNGramEmbedding._compute_ngram_embeddings`: Python `for i in range(seq_len)` loop over dynamic tensor length. Breaks graph tracing (AutoGraph can unroll only static ranges); O(seq_len) Python overhead. **Severity: XLA-compat.** Must be vectorized (as in `components.py:94` version) before any relocation.

4. **`modern_bert/modern_bert_blt_hrm.py:221`** — `HashNGramEmbedding.build`: `if self.built: return` guard. `built` is a managed property in Keras 3 (`Layer.__call__` sets it atomically); user code setting or checking it is a Keras 3 anti-pattern that can interfere with re-entrancy. **Severity: footgun.** Remove on deletion.

5. **`modern_bert/modern_bert_blt_hrm.py:358`** — `ReasoningByteEmbeddings.build`: creates Keras sublayers (`Embedding`, etc.) inside `build()` rather than `__init__`. Breaks `get_config`/`from_config` serialization round-trip — sublayers do not exist until `build()` is called, so they are invisible to the serialization machinery. **Severity: serialization.** Fix on next touch.

6. **`darkir/model.py:343`** — `FreMLP.call`: `_, h, w, _ = ops.shape(x)` then `s=(h, w)` passed to `ops.fft.irfft2`. Under JAX/XLA, `ops.shape` returns symbolic tensors; using them as a Python tuple for `irfft2`'s `s` argument may cause a trace failure or incorrect shape dispatch. Prefer static `x.shape[1]`, `x.shape[2]` at trace time. **Severity: XLA-compat.** Fix on relocate to `FreMLP`.

7. **`nano_vlm_world_model/scheduler.py:20`** — `DiffusionScheduler`: subclasses `keras.layers.Layer` with no `call()` override and no trainable weights. Invoking as a layer returns `None` (base class `call`). No functional bug today (never called via `layer(x)`) but is a latent silent-None footgun. The correct pattern is a plain Python class (cf. `sd3_mmdit/scheduler.py:51 FlowMatchEulerScheduler`). **Severity: footgun.** Refactor to plain Python class on next touch.

8. **`ideogram4/vae.py:263`** — `AttnBlock.call`: `ops.stack([b, n, c])` where all three are symbolic dims (from `ops.shape`). `ops.stack` returns a 1D tensor of symbolic ints, which is unconventional as a reshape target and may behave incorrectly under XLA reshape dispatch. Prefer `ops.reshape(q, (b, n, c))` directly. **Severity: XLA-compat.** Fix on relocate to `VAEAttnBlock`.

9. **`dino/dino_v1.py:176–267`** — `DINOHead.build`: all sublayers (`mlp_layers` list, `last_layer`) created in `build()` not `__init__`. Violates Keras 3 Create-vs-Build rule; sublayers are invisible to `get_config`/`from_config`, breaking serialization of any model that saves a `DINOHead`. **Severity: serialization.** Fix on next touch to `DINOHead`.

10. **`depth_anything/components.py:149–208`** — `DPTDecoder.build`: all sublayers (`conv_layers`, `batch_norm_layers`, `activation_layers`, `upsample_layers`, `output_conv`) created in `build()` not `__init__`. Same Keras 3 anti-pattern; additionally `self.conv_layers = []` clear at line 159 would clobber rebuilt sublayers on double-build. **Severity: serialization / correctness on double-build.** Fix on next touch.

11. **`memory_bank/write_controller.py:111–117`** — `MemoryWriteController.call`: uses `tf.debugging.assert_less_equal` directly (raw TF op). Documented as an intentional TF-backend workaround for `ops.pad` dynamic-paddings tracing failure, but it hard-binds the layer to the TF backend. Not a functional bug on TF but breaks JAX/PyTorch backends. **Severity: portability.** Replace with `keras.ops`-compatible guard when multi-backend support is needed.

## Recommendations (prioritized)

These are recommendations for a **future plan**. This audit is report-only; nothing under `src/` was changed.

---

### Tier 1 — Drop-in REPLACEs (delete duplication, near-zero risk)

Four inline classes are byte-for-byte or config-for-config equivalent to existing `layers/` classes. No `layers/` modification required. Delete the inline class and add an import.

- **`_LayerScale1D` (`cliffordnet/clip.py:153`)** → import `LearnableMultiplier` from `layers/layer_scale.py`. Pass `multiplier_type='CHANNEL'`, `initializer=Constant(1e-5)`, `constraint=None`. Add a checkpoint shim for existing `Custom>_LayerScale1D` saves.
- **`DINOv2Block` (`dino/dino_v2.py:68`)** → import `TransformerLayer` from `layers/transformers/transformer.py`. Config confirmed bit-for-bit equivalent with `pre-norm`, `use_layer_scale=True`, `layer_scale_init_value=init_values`, `use_stochastic_depth=True`. The block wrapper is pure duplication.
- **`ByteTokenizer` (`modern_bert/components.py:10`)** → import `ByteTokenizer` from `layers/blt_blocks.py`. Unify on `errors='replace'` for robustness. Stateless; no checkpoint concern.
- **`TRMReasoningModule` (`tiny_recursive_model/components.py:47`)** → import `HierarchicalReasoningModule` from `layers/reasoning/hrm_reasoning_module.py`. Identical parameter names and injection mechanism; confirmed bit-for-bit equivalent. Update `TRMInner` callers.

---

### Tier 2 — One-param-gap REPLACEs (tiny `layers/` additions unlock multiple deletions)

Small extensions to existing `layers/` classes turn needs-adapter rows into drop-ins and eliminate two to three inline classes per change.

- **Add `use_bias: bool = True` to `GatedGeometricResidual` (`layers/geometric/clifford_block.py:287`):** Deletes both `BiasFreeGatedGeometricResidual` (`cliffordnet/denoiser.py:246`) and `BiasFreeConditionedGGR` (`cliffordnet/conditional_denoiser.py:78`) — 2 deletions for 1 line change.
- **Add `norm_center: bool = True` to `CliffordNetBlock` (`layers/geometric/clifford_block.py:482`):** Deletes `BiasFreeClifordNetBlock` (`cliffordnet/denoiser.py:61`) — 1 deletion. Pair with the `use_bias` change above to cover the full bias-free cliffordnet surface.
- **Add `flatten: bool = True` to `PatchEmbedding2D` (`layers/embedding/patch_embedding.py:74`):** When `False`, skip reshape at line 247. Deletes `PatchEmbedding` in `sam/image_encoder.py:117` — 1 deletion. Also future-proofs for any model needing 4D patch features.
- **Add `use_token_type_ids: bool = True` and `sinusoidal_pos_embds: bool = False` to `BertEmbeddings` (`layers/embedding/bert_embeddings.py:80`):** Unifies `DistilBertEmbeddings` (`distilbert/model.py:82`) — 1 deletion. (Note: `DistilBertEmbeddings` is currently KEEP; this upgrade path unlocks a future REPLACE.)
- **Add `use_logits: bool = False` (or strip softmax path) to `ClassificationHead` (`layers/heads/vision/factory.py:633`):** Unifies `_ClassificationHeadBlock` (`cliffordnet/unet.py:83`) — 1 deletion.
- **Verify causal-mask forwarding through `create_attention_layer`:** Once confirmed, `CausalSelfAttnMLPBlock` (`video_jepa/predictor.py:63`) → `TransformerLayer(pre-norm, layer_scale=1e-5)` — 1 deletion.

---

### Tier 3 — High-value RELOCATEs (new shared subpackages)

Prioritized by number of current or near-term consumers.

1. **`layers/vae/` (new subpackage) — 4 classes, 2 multi-model consumers:** `ResnetBlock` and `AttnBlock` are already shared by ideogram4 and sd3_mmdit (2 callers each). Creating `layers/vae/` with `GroupNormResnetBlock`, `VAEAttnBlock`, `HierarchicalConditionalPrior`, `PoolDerivedGaussianHead` eliminates the cross-model duplication immediately. Fix appendix bugs #8 (AttnBlock `ops.stack`) during relocation.

2. **`layers/masking/` (new subpackage) — 2 classes, cross-SSL:** `TubeMaskGenerator` (V-JEPA) and `PatchMasking` (MAE) implement the same argsort-based masking idiom and are directly cited as related in source comments. A shared `layers/masking/` subpackage makes SimMIM/BEiT/I-JEPA extensions trivial.

3. **`layers/fire_module.py` — 2 classes, squeezenet package currently reuse-blind:** `FireModule` and `SimplifiedFireModule` currently import nothing from `layers/`. Relocating both to a single file makes the squeezenet package consistent with the rest of the library.

4. **Clifford geometric extensions (`layers/geometric/clifford_block.py`) — 3 classes:** `ConditionedCliffordNetBlock`, `BiasFreeGeometricDownsample`, `BiasFreeGeometricUpsample` extend an existing file and fix the additive-conditioning bug (appendix #1). Any future Clifford encoder/U-Net benefits immediately.

5. **`layers/ffn/normed_mlp.py` — `NormedMLPProjector`, factory key `normed_mlp`:** The mid-MLP LayerNorm pattern (`MLPProjector`) is the standard SSL projector idiom (I-JEPA, V-JEPA, LeWM). Registering a factory key makes it accessible to any model via `create_ffn_layer('normed_mlp', ...)`.

6. **`layers/embedding/` — 3 new embedding primitives:** `HashNGramEmbedding` (fix Python-loop bug, appendix #3 before relocating), `PositionalEncodingSine1D`, `PositionEmbeddingRandom`. Each fills a confirmed gap in the 10-key embedding factory.

7. **`layers/sr_residual_block.py` + `layers/rdn_dense_block.py` — SR primitives:** `EDSRResidualBlock`, `RDBConv`, `RDB` are standard SR building blocks; any future EDSR/RDN/THERA variant would benefit.

---

### Tier 4 — Bug fixes to bundle with any relocation or replacement

Never carry a bug forward into `layers/`. Bundle the relevant appendix fix with each action:

- Relocating `BiasFreeConditionedCliffordBlock` → fix additive conditioning bias-free violation (appendix #1).
- Relocating `DiscreteConditioningInjection` → fix in-`call` sublayer construction (appendix #2).
- Relocating `HashNGramEmbedding` (components.py version) → confirm vectorized hashing before promoting; the `blt_hrm.py` version (appendix #3, #4) must be deleted, not merged.
- Relocating `FreMLP` → fix symbolic-shape `irfft2` bug (appendix #6).
- Relocating `AttnBlock` → fix `ops.stack` reshape bug (appendix #8).
- On next touch to `DINOHead` → fix sublayer-in-build anti-pattern (appendix #9).
- On next touch to `DPTDecoder` → fix sublayer-in-build + double-build clobber (appendix #10).
- On next touch to `DiffusionScheduler` → refactor to plain Python class (appendix #7).
- On next touch to `ReasoningByteEmbeddings` → fix sublayer-in-build (appendix #5).

---

### Closing note — intra-models duplicates to dedup independently

The following duplications exist within `models/` itself and are worth cleaning up regardless of the `layers/` migration:

- **`HashNGramEmbedding` ×2:** `modern_bert/components.py:94` (canonical, vectorized, SUM) vs `modern_bert/modern_bert_blt_hrm.py:191` (buggy Python loop, AVERAGE). Promote `components.py` version to `layers/`, delete `blt_hrm.py` version, update `ReasoningByteEmbeddings` import.
- **`ConvUNextStem` ×2:** `bias_free_denoisers/bfconvunext.py:59` (GRN+GELU, `use_bias=False` hardcoded) vs `convunext/model.py:39` (LN, `use_bias` param, no GELU). These are related but distinct; a single parameterized `BiasFreeConvStem` could unify them only if a third caller appears — leave as KEEP for now, track.
- **`ByteTokenizer` cross-package:** `modern_bert/components.py:10` duplicates `layers/blt_blocks.py:232`. Tier-1 drop-in REPLACE above eliminates the `models/` copy.

---

*Self-consistency check: REPLACE list = 11 rows; RELOCATE list = 25 rows; KEEP = 59; total = 95. Batch tallies: Batch0 14 [5R/6Reloc/3K] + BatchA 21 [2R/2Reloc/17K] + BatchB 15 [0R/6Reloc/9K] + BatchC 12 [0R/4Reloc/8K] + BatchD 13 [3R/3Reloc/7K] + BatchE 20 [1R/4Reloc/15K] = 95. All consistent.*

## Findings by family

### Batch 0 — bias_free_denoisers + cliffordnet (14 classes)

| Class | file:line | Verdict | Target / proposed home | Drop-in? | Evidence (both sides) | Notes |
|---|---|---|---|---|---|---|
| `ConvUNextStem` | `bias_free_denoisers/bfconvunext.py:59-131` | KEEP | — | — | inline: `Conv2D(use_bias=False)` → `GlobalResponseNormalization` → `gelu` (3-op stem). Candidates `layers/convnext_v2_block.py:60` & `layers/convnext_v1_block.py:54` are DepthwiseConv + MLP-bottleneck residual blocks — different topology. | Reusable across bias-free U-Nets; promote to `BiasFreeConvStem` only if a second caller appears. |
| `DenseConditioningInjection` | `bias_free_denoisers/bfunet_conditional_unified.py:112-243` | RELOCATE | `layers/` (or `layers/ffn/`) as **BiasFreeFeatureModulation** | — | inline: two-input bias-free FiLM (film/multiplication/concatenation; `addition` rejected). Absence: `grep -rn "film\|FiLM\|conditioning.*inject"` in `layers/` → no FiLM/conditioning-injection layer exists. | Bias-free multiplicative-only modulation; cross-cutting for any bias-free U-Net / VAE / diffusion model. |
| `DiscreteConditioningInjection` | `bias_free_denoisers/bfunet_conditional_unified.py:247-375` | RELOCATE | `layers/` as **BiasFreeEmbeddingBroadcast** | — | inline: embedding → spatial broadcast (`spatial_broadcast` / `channel_concat`). Absence: `layers/sequence_pooling/` pools sequences, `layers/heads/` output predictions — neither does mid-network spatial broadcast. | Standard in conditional diffusion / generative U-Nets. **BUG (for appendix):** constructs `RepeatVector`/`Reshape` inline in `call` (new layer objects every forward pass) — anti-pattern, fix on relocate. |
| `_ClassificationHeadBlock` | `cliffordnet/unet.py:83-173` | REPLACE | `layers/heads/vision/factory.py:633` (`ClassificationHead`) | needs-adapter | inline: GAP → `LayerNorm(1e-6)` → opt Dropout → opt `Dense(gelu)` → `Dense(num_classes)`, returns **bare logits**. Candidate applies `softmax` and returns `{'logits','probabilities'}` dict. Same math, different output contract. | Output-contract trap (rule 3): needs softmax stripped + dict unwrapped, or a `use_logits=True` mode on `ClassificationHead`. Not drop-in. |
| `_SpatialHeadBlock` | `cliffordnet/unet.py:177-259` | KEEP | — | — | inline: `LayerNorm(1e-6)` → opt `Conv2D(3, gelu)` → `Conv2D(1)`, raw values. Candidates `DepthEstimationHead`/`SegmentationHead` (`layers/heads/vision/factory.py`) add task scaling / sigmoid / softmax + dict output. | Lightweight 3-op head; "raw values" contract intentionally differs from head factories. |
| `_DetectionHeadBlock` | `cliffordnet/unet.py:275-339` | KEEP | — | — | inline: thin wrapper over `layers/yolo12_heads.py` `YOLOv12DetectionHead` (lazy import + list/tuple validation + unused `use_bias`). | Glue/adapter for the `CliffordNetUNet` head protocol; adds no math. Note: inline-able — callers can use `YOLOv12DetectionHead` directly. |
| `BiasFreeClifordNetBlock` | `cliffordnet/denoiser.py:61-237` | REPLACE | `layers/geometric/clifford_block.py:482` (`CliffordNetBlock`) **+ needs `norm_center` param** | needs-adapter | inline: identical dual-stream arch but `LayerNorm(center=False)`, `BN(center=False)`, `use_bias=False` throughout. Candidate already accepts `use_bias` (`clifford_block.py:575`) default True, `center=True`, configurable norm + bias init/reg. | `CliffordNetBlock` minus `center` parameterization. Add `norm_center: bool` to `CliffordNetBlock` (or promote this class alongside it). |
| `BiasFreeGatedGeometricResidual` | `cliffordnet/denoiser.py:246-333` | REPLACE | `layers/geometric/clifford_block.py:287/346` (`GatedGeometricResidual`) **+ needs `use_bias`** | needs-adapter | inline gate: `Dense(C, use_bias=False)` (`denoiser.py:282`). Candidate gate: `Dense(C, use_bias=True)` (`clifford_block.py:375`); gamma init / drop_path / silu+gate formula bit-for-bit identical. | One-param gap (rule 2). Add `use_bias: bool = True` to `GatedGeometricResidual` → this class becomes redundant. |
| `_LayerScale1D` | `cliffordnet/clip.py:153-194` | REPLACE | `layers/layer_scale.py:97` (`LearnableMultiplier`) | drop-in | inline: `build` adds `gamma (channels,)` init `init_value`; `call: x * gamma`. Candidate `LearnableMultiplier(multiplier_type='CHANNEL', initializer=Constant(1e-5), constraint=None)` does `ops.multiply(inputs, gamma)`, gamma shape `(input_shape[-1],)`. | Drop-in with explicit args (defaults differ: `constraint='non_neg'`→None, `initializer='ones'`→`Constant(1e-5)`). Checkpoint shim needed for existing `Custom>_LayerScale1D` saves. |
| `_LearnedQueryPool1D` | `cliffordnet/clip.py:198-264` | RELOCATE | `layers/sequence_pooling/` as **LearnedQueryPool** | — | inline: learnable `query (channels,)`, scaled dot-product `einsum("bnd,d->bn")/sqrt(C)` + opt mask + softmax → `(B,D)`. Candidate `layers/sequence_pooling/attention_pooling.py` uses `tanh(Wx+b)·u` (Lin et al. 2017) — **different math**. | Same-name-different-math trap (rule 4): NOT `SequencePooling('attention')`. Reusable class-token-free attention pool (CLIP/EVA). Preserve the `mask` arg on relocate. |
| `BiasFreeConditionedGGR` | `cliffordnet/conditional_denoiser.py:78-178` | REPLACE | `layers/geometric/clifford_block.py` (`GatedGeometricResidual`) **+ needs `use_bias`** | needs-adapter | inline: byte-for-byte equivalent to `BiasFreeGatedGeometricResidual` (`denoiser.py:246-333`) — `Dense(C, use_bias=False)` gate, LayerScale gamma, opt drop_path; adds `get_build_config`/`build_from_config`. "conditioned" in name but no conditioning in this class. | Duplicate (same param gap). Once `GatedGeometricResidual` gains `use_bias`, both this and `BiasFreeGatedGeometricResidual` are dead code. |
| `BiasFreeConditionedCliffordBlock` | `cliffordnet/conditional_denoiser.py:187-451` | RELOCATE | `layers/geometric/clifford_block.py` as **ConditionedCliffordNetBlock** | — | inline: `CliffordNetBlock` arch + `use_bias=False`/`center=False` + two optional conditioning arms (`cond_gamma_proj` multiplicative FiLM, `class_proj` additive class-embedding broadcast). Candidate `CliffordNetBlock` (`clifford_block.py:482`) lacks `enable_dense_conditioning`/`enable_discrete_conditioning`. | RELOCATE not REPLACE: conditioning flags absent from `CliffordNetBlock`. **BUG (for appendix):** discrete conditioning is injected **additively** (`x_norm = x_norm + cls_feat`, `conditional_denoiser.py:391`) despite docstring claiming all modulations multiplicative — violates bias-free property; fix on relocate, do not preserve. |
| `BiasFreeGeometricDownsample` | `cliffordnet/conditional_denoiser.py:460-619` | RELOCATE | `layers/geometric/clifford_block.py` | — | inline: ctx stream `DWConv(s1)→DWConv(s2)→BN(center=False)→SiLU`; detail `Dense(bias=False)→AvgPool(2)`; fusion `SparseRollingGeometricProduct→Conv2D(1,bias=False)→BN(center=False)`. Absence: `layers/downsample.py` is strided-Conv2D/MaxPool only — no Clifford geometric product. | Novel geometric strided-downsample primitive; general to any Clifford encoder. Belongs alongside `SparseRollingGeometricProduct`. |
| `BiasFreeGeometricUpsample` | `cliffordnet/conditional_denoiser.py:628-766` | RELOCATE | `layers/geometric/clifford_block.py` | — | inline: `UpSampling2D(2,'nearest')` → opt `Conv2D(1,bias=False)` channel transition → `BiasFreeConditionedCliffordBlock` (conditioning disabled) refinement. Absence: no geometric-aware upsample in `layers/`. | Symmetric to `BiasFreeGeometricDownsample`. Belongs in `layers/geometric/clifford_block.py`. |

**Bugs noted for Synthesis appendix:** (1) additive discrete-conditioning bias-free violation at `cliffordnet/conditional_denoiser.py:391` (`x_norm = x_norm + cls_feat`); (2) in-`call` sublayer construction anti-pattern in `DiscreteConditioningInjection` (`RepeatVector`/`Reshape` built per forward pass instead of in `__init__`/`build`).

### Batch A — transformer/LLM family (21 classes)

| Class | file:line | Verdict | Target / proposed home | Drop-in? | Evidence (both sides) | Notes |
|---|---|---|---|---|---|---|
| `Qwen3NextBlock` | `qwen/components.py:31` | KEEP | — | — | inline: 3×(ZeroCenteredRMSNorm→GatedDeltaNet→opt MoE→opt StochasticDepth→residual) + 1×(RMSNorm→GatedAttention→opt MoE→residual). `layers/transformers/transformer.py:94` (`TransformerLayer`) has one Attention+FFN cycle only; `GatedDeltaNet` not in any factory. Already uses `layers/moe`, `layers/stochastic_depth`, `layers/norms` factory, `layers/attention/gated_attention`. | Bespoke 3+1 DeltaNet/Attention topology; inexpressible via TransformerLayer config. All possible sub-components already reuse canonical layers/. |
| `MemoryIntegrationLayer` | `qwen/qwen3_mega.py:39` | KEEP | — | — | inline: dual cross-attention (memory + entity) → concat 3 streams → Dense(gelu) → LayerNorm → residual. `TransformerDecoderLayer` handles single cross-attn pair; `layers/fusion/multimodal_fusion.py` does modality fusion, not dual-memory integration. | Qwen-MEGA-specific dual-memory cross-attention fusion; no equivalent in layers/. |
| `Qwen3EmbeddingLayer` | `qwen/qwen3_embeddings.py:86` | KEEP | — | — | inline: full causal encoder (Embedding + PositionalEmbedding + N×TransformerLayer + norm → last-token pool → MRL truncation → L2 norm). Already delegates to embedding factory, TransformerLayer, norm factory. | Full model-level composite; all sub-components already use canonical layers/. |
| `Qwen3RerankerLayer` | `qwen/qwen3_embeddings.py:352` | KEEP | — | — | inline: same causal encoder as Qwen3EmbeddingLayer + lm_head Dense(vocab) + yes/no softmax score extraction. Already uses same factories. No binary-scoring LM head in heads/nlp/. | Qwen3-specific generative yes/no reranker model; not a general primitive. |
| `Gemma3TransformerBlock` | `gemma/components.py:25` | KEEP | — | — | inline: DUAL-post-norm (pre_norm+post_attn_norm on attn, pre_ffn_norm+post_ffn_norm on FFN) → 4 RMSNorm layers. `TransformerLayer` (`transformer.py:94`) supports `normalization_position='pre'|'post'` — ONE norm per sublayer, not two. Already uses `create_normalization_layer('rms_norm')`, `create_attention_layer('group_query')`, `create_ffn_layer('geglu')`. | Dual-post-norm pattern inexpressible via TransformerLayer. All sub-components already reuse canonical layers/ factories. |
| `DistilBertEmbeddings` | `distilbert/model.py:82` | KEEP | — | — | inline: word_embed + optional position_embed (learned or sinusoidal) + norm + dropout. No token-type embed by design. `layers/embedding/bert_embeddings.py:80` (`BertEmbeddings`) requires `type_vocab_size`, always creates token_type_embeddings; no `use_token_type_ids=False` flag; no `sinusoidal_pos_embds` param. Already uses `layers/norms/RMSNorm`. | Deliberate architectural subset of BERT; two missing params block unification. Would require `use_token_type_ids: bool` + `sinusoidal_pos_embds: bool` added to BertEmbeddings. |
| `ByteTokenizer` | `modern_bert/components.py:10` | REPLACE | `layers/blt_blocks.py:232` (`ByteTokenizer`) | drop-in | inline: `vocab_size=260, byte_offset=4`, pad=0, bos=1, eos=2, unk=3, stateless. Candidate `layers/blt_blocks.py:232` identical interface (pad=0, bos=1, eos=2, sep=3 — same ID). Both `trainable=False`. Minor diff: `modern_bert` uses `errors='replace'` on encode+decode; `blt_blocks` uses `errors='ignore'` on encode. | Drop-in for all valid UTF-8. Unify on `errors='replace'` for robustness. |
| `HashNGramEmbedding` (components.py) | `modern_bert/components.py:94` | RELOCATE | `layers/embedding/hash_ngram_embedding.py` as **HashNGramEmbedding**; register factory key `hash_ngram` | — | inline: `keras.layers.Embedding` dict sublayers (idiomatic), vectorized padded-slice hashing (graph-mode safe), SUM aggregation. Absent from `layers/` (searched blt_core.py + blt_blocks.py). This is the canonical version vs the blt_hrm.py duplicate. | Promote this version; delete blt_hrm.py:191 version. General byte-ngram primitive reusable across any byte-level NLP model. |
| `HashNGramEmbedding` (blt_hrm.py) | `modern_bert/modern_bert_blt_hrm.py:191` | REPLACE | `layers/embedding/hash_ngram_embedding.py` (post-relocation of components.py version) | needs-adapter | inline: `add_weight` raw weight dict (non-idiomatic), sequential Python `for i in range(seq_len)` hashing loop (graph-tracing anti-pattern), AVERAGE aggregation. Confirmed intra-package dup of components.py:94. | **BUG:** Python loop over dynamic seq_len breaks graph tracing. Delete after components.py version is promoted. |
| `ModernBertBltEmbeddings` | `modern_bert/components.py:223` | KEEP | — | — | inline: ByteTokenizer + byte_embed + position_embed + optional HashNGramEmbedding + optional Dense projection + LayerNorm + Dropout + encode_text/decode_tokens API. No byte-level BERT embedding composite in layers/. | ModernBERT-BLT-specific composite; update imports after ByteTokenizer + HashNGramEmbedding are relocated. |
| `ReasoningByteEmbeddings` | `modern_bert/modern_bert_blt_hrm.py:313` | KEEP | — | — | inline: ByteTokenizer + byte_embed + optional SparsePuzzleEmbedding (ARC-specific) + optional RoPE or PositionalEmbedding + optional HashNGramEmbedding + norm + dropout. ARC puzzle-context embedding. Already imports RMSNorm, RotaryPositionEmbedding, PositionalEmbedding from layers/. **BUG:** creates sublayers in `build` not `__init__` — breaks serialization (see batch-A findings). | ARC-specific puzzle-context embedding; not a general primitive. Fix sublayer creation on next edit. |
| `ReasoningByteCore` | `modern_bert/modern_bert_blt_hrm.py:527` | KEEP | — | — | inline: full ARC reasoning pipeline (EntropyModel + DynamicPatcher + LocalEncoder + PatchPooling + 2×HierarchicalReasoningModule + lm_head + q_head halt-decision). Deferred sublayer creation in build. | Full ARC reasoning model; not a reusable primitive. |
| `PositionalEncoding` | `tree_transformer/components.py:32` | RELOCATE | `layers/embedding/positional_encoding_sine_1d.py` as **PositionalEncodingSine1D**; register factory key `sine_1d` | — | inline: fixed sinusoidal PE `(1, max_len, d)` non-trainable weight, `call: x + pe[:, :seq_len, :]` + Dropout. Absence confirmed: `positional_learned` is learnable; `continuous_sincos` is multi-dim continuous; `scalar_sinusoidal` is scalar→vector; `PositionEmbeddingSine2D` is 2D spatial. No fixed 1D discrete PE exists in layers/. | General Vaswani-et-al. sinusoidal PE; reusable by any non-RoPE transformer. |
| `GroupAttention` | `tree_transformer/components.py:153` | KEEP | — | — | inline: adjacency-masked dot-product → symmetrize → CKY log-space DP → `g_attn (B,S,S)` group prob + `neibor_attn (B,S,S)`. None of 23 attention factory keys implement tree-induction DP. `group_query` key = GQA (fewer KV heads) — different math. | Novel CKY-style tree induction; no equivalent in attention factory. |
| `TreeMHA` | `tree_transformer/components.py:347` | KEEP | — | — | inline: standard scaled dot-product MHA + `attention_weights *= expand_dims(group_prob, 1)` (structure modulation). `MultiHeadAttention` (`layers/attention/multi_head_attention.py`) has no `group_prob` modulation parameter in call. | Structure-modulated MHA; group_prob multiplicative gate inexpressible via any existing attention layer. |
| `TreeTransformerBlock` | `tree_transformer/components.py:504` | KEEP | — | — | inline: `GroupAttention → norm1 → TreeMHA → dropout → residual → norm2 → create_ffn_layer → dropout → residual`; I/O is 3-tuple `(x, mask, prior)` → `(x_out, g_attn, break_prob)`. `TransformerLayer` takes single tensor, returns single tensor; GroupAttention and TreeMHA are not factory-addressable. Already uses `create_ffn_layer`, `create_normalization_layer`. | 3-tuple I/O + bespoke sub-layers; inexpressible via TransformerLayer. Sub-components maximally reuse layers/ factories. |
| `WaveFieldDecoderBlock` | `wave_field_llm/wave_field_llm.py:67` | KEEP | — | — | inline: pre-norm `LayerNorm→WaveFieldAttention→residual; LayerNorm→Dense(4D,gelu)→Dense(D)→Dropout→residual`. `WaveFieldAttention` (`layers/attention/wave_field_attention.py`) is NOT in the 23-key attention factory. `TransformerLayer(attention_type='wave_field')` is not possible. Sub-component miss: inline FFN (Dense×2) could use `create_ffn_layer('mlp', hidden_dim=4*D, output_dim=D)`. | WaveFieldAttention not in factory; block is inexpressible via TransformerLayer. Minor: inline FFN could be replaced by `layers/ffn/mlp.py:MLPBlock`. |
| `MambaLayer` | `mamba/components.py:15` | KEEP | — | — | inline: selective SSM (data-dependent Δ/B/C), causal Conv1D, S4D A_log init, while_loop scan, SiLU gating. Zero import from layers/. No SSM primitive in any layers/ subpackage. | Novel selective SSM; no equivalent in layers/. |
| `MambaResidualBlock` | `mamba/components.py:554` | KEEP | — | — | inline: `LayerNorm(epsilon)→MambaLayer→residual`; returns `(mamba_output, new_residual)` tuple. No `TransformerLayer` equivalent for SSM. Sub-component: `LayerNormalization` could be `create_normalization_layer('layer_norm')` for factory consistency. | Pre-norm SSM residual wrapper; SSM not in any layers/ factory. |
| `Mamba2Layer` | `mamba/components_v2.py:10` | KEEP | — | — | inline: parallel Δ/B/C projection, gated MLP path, `LayerNormalization(rms_scaling=True)` on SSM output, gate modulation. No SSM v2 in layers/. | Mamba v2 parallel SSM; no equivalent in layers/. |
| `Mamba2ResidualBlock` | `mamba/components_v2.py:324` | KEEP | — | — | inline: `LayerNormalization(rms_scaling=True)→Mamba2Layer→residual`; returns `(mamba_output, new_residual)`. Sub-component: `LayerNormalization(rms_scaling=True)` could be `layers/norms/rms_norm.py:RMSNorm` for canonical reuse. | Pre-norm SSM v2 residual wrapper. Minor: swap to canonical RMSNorm from layers/norms/. |

**Bugs noted for Synthesis appendix (Batch A):**
(3) `modern_bert/modern_bert_blt_hrm.py:191` `HashNGramEmbedding._compute_ngram_embeddings`: Python `for i in range(seq_len)` loop over dynamic tensor length — breaks graph tracing, O(seq_len) Python overhead. Must be fixed (vectorized) on relocation.
(4) `modern_bert/modern_bert_blt_hrm.py:221` `HashNGramEmbedding.build` uses `if self.built: return` guard — Keras 3 anti-pattern; `built` is managed by `Layer.__call__`, not user code.
(5) `modern_bert/modern_bert_blt_hrm.py:358` `ReasoningByteEmbeddings.build` creates Keras sublayers (Embedding, etc.) inside `build` rather than `__init__` — breaks `get_config`/`from_config` serialization round-trip (sublayers don't exist until `build` is called).

### Batch B — vision/conv family (15 classes)

| Class | file:line | Verdict | Target / proposed home | Drop-in? | Evidence (both sides) | Notes |
|---|---|---|---|---|---|---|
| `FireModule` | `squeezenet/squeezenet_v1.py:79` | RELOCATE | `layers/fire_module.py` as **FireModule** | — | inline: `Conv2D(s1x1,1x1,relu)` [squeeze] → parallel `Conv2D(e1x1,1x1,relu)` + `Conv2D(e3x3,3x3,same,relu)` → `Concatenate`; output `e1x1+e3x3` channels. Candidate `layers/squeeze_excitation.py:73` (`SqueezeExcitation`) is GAP→Conv→Conv→sigmoid→multiply-back — channel-attention recalibration, NOT dimensionality-changing squeeze-expand. Different op. Absent from layers/ (searched `fire_module`, `squeeze.*expand`, `inverted_residual`, convnext blocks). | SqueezeNet canonical block; no layers/ equivalent. Distinct from SE-block despite shared "squeeze" terminology. Reusable for any efficient conv model. squeezenet package has zero layers/ imports — both Fire classes should import from layers/ once relocated. |
| `SimplifiedFireModule` | `squeezenet/squeezenet_v2.py:77` | RELOCATE | `layers/fire_module.py` as **SimplifiedFireModule** (alongside `FireModule`) | — | inline: `Conv2D(s1x1,1x1,relu)` → `Conv2D(e3x3,3x3,same,relu)` only; no parallel 1x1 expand; no Concatenate; output `e3x3` channels. Strict simplification of `FireModule` (`e1x1=0`). Same absence evidence as `FireModule`. Enforces `s1x1 < e3x3` validation not present in `FireModule`. | Medical-imaging SqueezeNet variant; 3x3-only expand for spatial-aware feature learning. Reusable. Co-locate with `FireModule` in same file. |
| `ConvUNextStem` | `convunext/model.py:39` | KEEP | — | — | inline: `Conv2D(filters, kernel_size, same, use_bias=use_bias)` → `LayerNormalization(epsilon=1e-6)`. Cluster 1 twin of `bias_free_denoisers/bfconvunext.py:59` but differs by: (a) LN not GRN, (b) `use_bias` param vs hardcoded False, (c) no GELU activation. Candidates `layers/convnext_v1_block.py:54` / `layers/convnext_v2_block.py:60` are DepthwiseSeparable+MLP bottleneck blocks — different topology. `layers/hierarchical_mlp_stem.py` is MLP-based. | 2-op stem with `use_bias` param; only one caller. Promote to RELOCATE if a third caller appears. Sub-component: norm could use `create_normalization_layer('layer_norm', epsilon=1e-6)` for factory consistency. |
| `ConvNeXtPatchEncoder` | `convnext_patch_vae/encoder.py:47` | KEEP | — | — | inline: patchifying `Conv2D(embed_dim, patch_size, stride=patch_size, valid)` → `LayerNorm` → N×`ConvNextV2Block` (imported from `layers/convnext_v2_block.py`) → `mu_head` (Conv2D 1x1, Glorot) + `log_var_head` (Conv2D 1x1, zeros-init). Already reuses `ConvNextV2Block`. `layers/sampling.py:73 Sampling` is reparameterization only, not encoder. | VAE-specific dual-head structure with plan-locked D-003 zero-init; resolution-agnostic constraint (D-001). One caller. Correctly reuses ConvNextV2Block. |
| `ConvNeXtPatchDecoder` | `convnext_patch_vae/decoder.py:39` | KEEP | — | — | inline: `Conv2D(embed_dim,1)` → N×`ConvNextV2Block` (imported from layers/) → `LayerNorm` → `Conv2DTranspose(img_channels, patch_size, stride=patch_size, valid)`. Raw logits. Already reuses `ConvNextV2Block`. | Symmetric decoder; pixel-expansion via Conv2DTranspose is VAE-specific. One caller. Correctly reuses ConvNextV2Block. |
| `_L2ConditionalPrior` | `convnext_patch_vae/model_hierarchical.py:50` | RELOCATE | `layers/` or `layers/vae/` (new subpackage) as **HierarchicalConditionalPrior** | — | inline: `UpSampling2D(up_factor,nearest)` → `Conv2D(embed_dim,1)` → N×`ConvNextV2Block` (imported) → `LayerNorm` → `mu_head` (zeros-init) + `log_var_head` (zeros-init). Returns `(mu_p, log_var_p)`. Absent: no hierarchical-VAE prior layer in layers/ (searched `ConditionalPrior`, `ladder.*vae`, `top_down`). D-002: zero-init mandatory. | Top-down conditional prior for Ladder-VAE/VDVAE family. Reusable in any hierarchical VAE. Resolution-agnostic constraint (D-001) must be documented in relocated class. |
| `_CoarseLatentHead` | `convnext_patch_vae/model_hierarchical.py:293` | RELOCATE | `layers/` or `layers/vae/` (new subpackage) as **PoolDerivedGaussianHead** | — | inline: `AveragePooling2D(pool_factor, stride=pool_factor, valid)` → `mu_head` (Conv2D 1x1) + `log_var_head` (Conv2D 1x1, zeros-init). Returns `(mu2, log_var2)`. Absent: no pool-derived Gaussian head in layers/ (searched `CoarseLatent`, `pool.*head`, `coarse.*gaussian`). D-001: only parameter-free pool + 1x1 convs allowed. | Lightweight 3-op pool-derived coarse Gaussian head; reusable for any hierarchical VAE deriving a coarse latent from fine features via pooling. |
| `SimpleGate` | `darkir/model.py:92` | RELOCATE | `layers/simple_gate.py` or `layers/activations/` as **SimpleGate** | — | inline: `x1, x2 = ops.split(x, 2, axis=-1); return x1 * x2`. Zero parameters. 4D NHWC. Candidate `layers/ffn/glu_ffn.py:85` (`GLUFFN`): Dense-based `gate_proj` + `value_proj` + `output_proj` with learned projections — not a parameter-free channel split. Different op. Candidate `layers/ffn/geglu_ffn.py`, `swiglu_ffn.py`: all Dense-based. Absent from layers/ (searched `ops\.split.*axis.*-1`, `SimpleGate`, `channel.*gate`). | NAFNet/DarkIR conv channel-split gate; parameter-free; operates on 4D spatial tensors. Distinct from Dense-based GLU/GeGLU in layers/ffn/ which have explicit projections. Reusable for any efficient CNN using this gating style. |
| `FreMLP` | `darkir/model.py:191` | RELOCATE | `layers/fft_layers.py` (alongside FFTLayer/IFFTLayer) or `layers/frequency_mlp.py` as **FreMLP** | — | inline: `ops.fft.rfft2(x, axes=(1,2))` → magnitude (`ops.abs`) + phase (`ops.angle`) → `Conv2D(hidden,1)` → `LeakyReLU(0.1)` → `Conv2D(C,1)` → reconstruct complex → `ops.fft.irfft2`. Shape-preserving. Candidate `layers/fft_layers.py:7 FFTLayer`: full-spectrum concat real+imag (channels doubled), no magnitude/phase decomposition, no inverse transform. Different op entirely. `layers/fnet_encoder_block.py`: 1D sequence token mixer, parameter-free. Absent: no rfft2/magnitude-MLP in layers/. **BUG:** `darkir/model.py:343` `_, h, w, _ = ops.shape(x)` then `s=(h, w)` in `irfft2` — symbolic tensors under JAX/XLA will fail; fix on relocate. | Frequency-domain magnitude-MLP for global context; O(HW logHW). Reusable for any CNN image restoration/enhancement. Distinct from FFTLayer/IFFTLayer (different frequency processing strategy). |
| `DilatedBranch` | `darkir/model.py:398` | KEEP | — | — | inline: single `Conv2D(channels*expansion, 3, same, dilation_rate=dilation, groups=dw_channels)`. One sub-layer. `layers/depthwise_separable_block.py`: full DW+PW+norm+act block — too composite. | Trivially thin single-conv parameterized by dilation rate; instantiated N times in a list inside `DarkIREncoderBlock`. Less useful as a standalone layer than inline `Conv2D`. |
| `DarkIREncoderBlock` | `darkir/model.py:562` | KEEP | — | — | inline: `LayerNorm` → optional extra DWConv → `Conv2D(expanded)` → N×`DilatedBranch` (sum) → `SimpleGate` → channel-attn (GAP+Conv2D) → multiply → `Conv2D(C)` → residual×`beta`; `LayerNorm` → `FreMLP` → elementwise-multiply → residual×`gamma`. Already uses `create_normalization_layer`. No equivalent composite in layers/. | DarkIR-specific composite; coupling multi-scale dilated extraction + SimpleGate + channel-attn + FreMLP. Sub-components SimpleGate + FreMLP are RELOCATE targets — once relocated, this block can import them from layers/. |
| `DarkIRDecoderBlock` | `darkir/model.py:908` | KEEP | — | — | inline: same Path 1 as encoder; Path 2 replaces FreMLP with `Conv2D(expanded)` → `SimpleGate` → `Conv2D(C)` + residual×`gamma`. Already uses `create_normalization_layer`. | DarkIR-specific decoder; gated FFN instead of FreMLP. Sub-components SimpleGate (2 instances) are RELOCATE targets. |
| `PW_FNet_Block` | `pw_fnet/model.py:36` | KEEP | — | — | inline: `Norm1` → `Conv2D(hidden)` → `FFTLayer` → `Conv2D(2*hidden)` → GELU → `IFFTLayer` → `Conv2D(dim)` → residual; `Norm2` → spatial-FFN (`Conv2D+DepthwiseConv2D+GELU+Conv2D`) or factory FFN → residual. Already imports `FFTLayer`/`IFFTLayer` from `layers/fft_layers`, `create_ffn_layer`, `create_normalization_layer`. Candidate `layers/fnet_encoder_block.py` (`FNetEncoderBlock`): 1D sequence token mixer `Real(FFT(FFT(x)))`, no conv expansion, different input format `(B,N,D)`. Different op. | Image-restoration FNet block; correctly reuses canonical FFT/IFFT layers, norms factory, FFN factory. PW-FNet architectural core; no equivalent in layers/transformers/. |
| `Downsample` | `pw_fnet/model.py:449` | KEEP | — | — | inline: `Conv2D(dim, 4, strides=2, same, bias=True)`. Single strided 4×4 conv. `layers/downsample.py:downsample()` function keys: `conv2d_2x2`, `conv2d_3x3` — no 4×4 variant. Also `layers/downsample.py` is a free function (takes live tensor, returns tensor) — not a `keras.layers.Layer`; cannot be stored as `self.downsample` sublayer. `layers/pixel_unshuffle.py`/`layers/patch_merging.py`: different ops. | 4×4 kernel not in DownsampleType; functional interface incompatible. Deferred: track for REPLACE if `layers/downsample.py` gains a `conv2d_4x4` key and is refactored to a Layer class. |
| `Upsample` | `pw_fnet/model.py:536` | KEEP | — | — | inline: `Conv2DTranspose(dim, 2, strides=2, same, bias=True)`. Single transpose conv. `layers/upsample.py:upsample()` has `conv2d_transpose` strategy but is a free function — same structural mismatch as `Downsample`. `layers/pixel_shuffle.py`: sub-pixel conv — different op. | Free-function interface incompatible with Layer subclass. Same reasoning as `Downsample`. Deferred: track for REPLACE if `layers/upsample.py` gains a Layer-class surface. |

**Bugs noted for Synthesis appendix (Batch B):**
(6) `darkir/model.py:343`: `_, h, w, _ = ops.shape(x)` → `s=(h, w)` in `ops.fft.irfft2` — symbolic tensors under JAX/XLA backends may cause failure or incorrect shape. Must be replaced with static shape derivation or concrete Python int at trace time. Fix on relocation of `FreMLP`.

### Batch C — VLM/multimodal/generative (12 classes)

| Class | file:line | Verdict | Target / proposed home | Drop-in? | Evidence (both sides) | Notes |
|---|---|---|---|---|---|---|
| `AttentionBlockVLM` | `fastvlm/components.py:15` | RELOCATE | `layers/transformers/` as **SpatialTransformerBlock** | yes (after relocation) | inline: reshape `(B,H,W,C)→(B,H*W,C)` → `TransformerLayer(hidden_size=dim, ...)` (imported from `layers/`) → reshape back → optional `LearnableMultiplier(CHANNEL)` (imported from `layers/`). Zero original math; both sub-components already canonical. Absence: no flatten-reshape wrapper around TransformerLayer in `layers/transformers/`; searched `SpatialTransformer`, `visual_transformer`, `AttentionBlock`. | Standard pattern for VLM vision backbone → transformer; applicable to any model that feeds 4D feature maps into a sequence transformer. Both sub-components (`TransformerLayer:94`, `LearnableMultiplier:97`) are already canonical. Already imported by fastvlm — only the reshape wrapper is inline. |
| `ImageProjectionHead` | `mobile_clip/components.py:19` | RELOCATE | `layers/heads/vlm/` as **ImageProjectionHead** | yes (after relocation) | inline: `GlobalAveragePooling2D` → `Dropout` → `Dense(projection_dim)` → optional activation. Input `(B,H,W,C)`, output `(B, projection_dim)`. Candidates: `layers/modality_projection.py:ModalityProjection` takes `(B,N,D)` + does PixelShuffle — different input format + op. `layers/heads/vlm/` heads are task-output heads, not embedding projectors. Absent from `layers/` (searched `ImageProjection`, `GAP.*projection`, `image.*embed`, vlm factory). | Standard CLIP/ALIGN/BLIP image-to-embedding projection head; reusable for any contrastive or VLM training. 3-op pipeline (GAP+Dropout+Dense). `layers/heads/vlm/` is the natural home alongside the existing VLM head family. |
| `MobileClipTextEncoder` | `mobile_clip/components.py:301` | KEEP | — | — | inline: `Embedding` + `PositionalEmbedding` (from `layers/`) + N×`TransformerLayer` (from `layers/`) + `LayerNormalization` + learnable `projection_weights` + EOT argmax+one_hot+matmul extraction. Full CLIP text encoder pipeline. `layers/transformers/text_encoder.py:114` (`TextEncoder`) is a TransformerLayer stack without CLIP-specific EOT pooling or matmul projection — different API contract. Already maximally reuses layers/. | Full CLIP text encoding composite (pipeline, not primitive). EOT extraction + matmul projection is CLIP-specific. One caller. All sub-components (TransformerLayer, PositionalEmbedding) already from layers/. |
| `DiffusionScheduler` | `nano_vlm_world_model/scheduler.py:20` | KEEP | — | — | inline: `keras.layers.Layer` subclass, zero trainable weights, no `call()` override, no `build()`. Precomputes DDPM schedule (betas, alphas, cumprod, posterior coefs) as numpy arrays in `__init__`. Methods (`add_noise`, `step`, `predict_start_from_noise`, `get_score_from_noise`) are stateless math utilities. Analogous `sd3_mmdit/scheduler.py:51` `FlowMatchEulerScheduler` is correctly a plain Python class. Absent from layers/: no DDPM/DDIM scheduler primitive (searched `Diffusion`, `DDPM`, `beta_schedule`). | **Structural caveat:** is-a-Layer questionable — zero weights, no `call()`, stateless math. `FlowMatchEulerScheduler` pattern (plain Python class) is the correct idiom. **BUG (7, for appendix):** `call()` not overridden; invoking this as a layer returns None (base class call). Refactor to plain Python class on next touch; then reconsider as shared utility if a second caller appears. |
| `MMDiTBlock` | `sd3_mmdit/blocks.py:141` | KEEP | — | — | inline: wires `MMDiTJointAttention` (from `layers/attention/`) + `AdaLayerNormZero`/`AdaLayerNormZeroX`/`AdaLayerNormContinuous` (from `layers/transformers/sd3_adaln`) + `GELUMLPFFN` (from `layers/ffn/`) + optional `keras.layers.MultiHeadAttention` (attn2, D-005) into a dual-stream image+text block. `layers/transformers/adaln_zero.py:67` (`AdaLNZeroConditionalBlock`) is SINGLE-stream; no dual-stream block anywhere in `layers/`. 3 structural variants via `context_pre_only` / `use_dual_attention` flags. | Bespoke dual-stream MMDiT block; inexpressible via any existing `layers/` block. All sub-components already use canonical layers/ (MMDiTJointAttention, AdaLN trio, GELUMLPFFN). AdaLN modulation fully covered by `layers/transformers/sd3_adaln.py`. |
| `MMDiTFinalLayer` | `sd3_mmdit/blocks.py:477` | KEEP | — | — | inline: `AdaLayerNormContinuous(dim)` (from `layers/transformers/sd3_adaln`) + `Dense(out_channels)`. 2-sublayer output head; maps `(B,N,dim)→(B,N,out_channels)` with conditioned norm. No existing "conditioned norm + projection" final layer in layers/. | Thin model-specific output head (2 ops). Already uses canonical `AdaLayerNormContinuous`. One caller (SD3MMDiT model). |
| `ResnetBlock` | `ideogram4/vae.py:76` | RELOCATE | `layers/vae/` (new subpackage) as **GroupNormResnetBlock** | yes (after relocation) | inline: `GroupNorm32→swish→Conv2D(3x3,same)→GroupNorm32→swish→Conv2D(3x3,same)` + optional 1x1 nin_shortcut (D-007: conditional only). `layers/standard_blocks.py:BasicBlock:736` uses BN not GN; `BottleneckBlock:973` is 1x1→3x3→1x1 — different topology. Absent from layers/ (searched `GroupNorm.*residual`, `ResnetBlock`, `conv_residual.*group`, `VAE.*residual`). **Intra-models reuse:** used by both ideogram4 + sd3_mmdit (via `ideogram4.AutoEncoder` D-002) — 2 callers already. | Flux2 KL-VAE GroupNorm residual block; reused across 2 generative models. No existing GroupNorm-based ResNet block in layers/. GroupNorm groups=32 constraint (channels divisible by 32) must be documented. |
| `AttnBlock` | `ideogram4/vae.py:191` | RELOCATE | `layers/vae/` (new subpackage) as **VAEAttnBlock** | yes (after relocation) | inline: `GroupNorm32` → 1×1 q/k/v `Conv2D` → flatten `(B,H,W,C)→(B,HW,C)` → scaled-dot-product attention (manual, no masking) → reshape → 1×1 `proj_out Conv2D` → residual. `layers/attention/` factory (23 keys): all consume `(B,N,D)` sequences, none use GroupNorm + Conv2D Q/K/V. Absent: no spatial VAE attention block with GroupNorm + 1×1 Conv Q/K/V in layers/. **BUG (8):** `ops.stack([b, n, c])` for reshape target mixes symbolic dims — unconventional, review for JAX/XLA correctness. **Intra-models reuse:** 2 callers (ideogram4, sd3_mmdit). | Flux2 KL-VAE bottleneck self-attention block; O((H*W)^2) memory — bottleneck-only constraint must be documented. Reused by 2 models. |
| `Downsample` | `ideogram4/vae.py:296` | KEEP | — | — | inline: asymmetric NHWC pad `[[0,0],[0,1],[0,1],[0,0]]` + `Conv2D(channels, 3, stride=2, valid)`. PyTorch `F.pad(0,1,0,1)` parity. D-005 confirmed in source (`vae.py:317` comment block). `layers/downsample.py` has no asymmetric-padding variant; functional interface incompatible. **RELOCATE consideration:** 2-model reuse (ideogram4 + sd3_mmdit) but D-005 prohibits replacement with functional builder. | D-005 locked (plan_2026-06-12_59a18a10). PyTorch-parity asymmetric padding cannot be expressed via `layers/downsample.py`. Even if extracted to `layers/vae/`, D-005 anchor comment must follow. |
| `Upsample` | `ideogram4/vae.py:366` | KEEP | — | — | inline: `UpSampling2D(2, nearest)` + `Conv2D(channels, 3, same)`. D-005 decision anchor embedded in class docstring (`vae.py:367-378`): explicitly prohibits replacement with `layers/upsample.upsample()` (functional builder). **RELOCATE consideration:** same 2-model reuse but decision-locked. | D-005 locked (plan_2026-06-12_59a18a10). Docstring states "NOT a call to `layers.upsample.upsample(...)`" explicitly. |
| `Encoder` | `ideogram4/vae.py:435` | KEEP | — | — | inline: full Flux2 KL-VAE encoder composite (`conv_in` → flat list ResnetBlock + Downsample → mid ResnetBlock + AttnBlock + ResnetBlock → `norm_out` GroupNorm32 → `conv_out` → `quant_conv`). No equivalent VAE encoder in layers/. Sub-components (ResnetBlock, AttnBlock, Downsample) are RELOCATE targets — once relocated, Encoder imports from layers/vae/. | Full VAE encoder pipeline; model-specific Flux2 architecture. One caller per pipeline (AutoEncoder). |
| `Decoder` | `ideogram4/vae.py:621` | KEEP | — | — | inline: full Flux2 KL-VAE decoder composite (`post_quant_conv` → `conv_in` → mid → flat list ResnetBlock + Upsample → `norm_out` → `conv_out`). No equivalent VAE decoder in layers/. Sub-components are RELOCATE targets. | Full VAE decoder pipeline; model-specific Flux2 architecture. Symmetric to Encoder. |

**Bugs noted for Synthesis appendix (Batch C):**
(7) `nano_vlm_world_model/scheduler.py:20` `DiffusionScheduler`: subclasses `keras.layers.Layer` with no `call()` override and no trainable weights. Invoking as a layer returns `None` (base class `call`). Should be a plain Python class (like `FlowMatchEulerScheduler` in `sd3_mmdit/scheduler.py:51`). No functional bug today (never called via `layer(x)`) but is a latent silent-None footgun.
(8) `ideogram4/vae.py:263` `AttnBlock.call`: `ops.stack([b, n, c])` where `b = shp[0]`, `n = hh * ww`, `c = shp[3]` are all symbolic. `ops.stack` produces a 1D tensor of symbolic ints, which is unconventional as a reshape target. Prefer `ops.reshape(q, (b, n, c))` directly (works because all dims are ops.shape outputs). Review for XLA reshape safety.

**GHOST constraint verification (Batch C):**
- D-005 (ideogram4 Downsample/Upsample locked): CONFIRMED ACTIVE — source `vae.py:317` and `vae.py:367-378` embed the anchor verbatim. Not a ghost.
- SYSTEM.md claim "sd3_mmdit reuses RMSNorm/PatchEmbedding2D/Sampling/ScalarSinusoidalEmbedding": CONFIRMED — these are reused in `sd3_mmdit/transformer.py:67-68`, NOT in `blocks.py`. The classes in Batch C (`MMDiTBlock`, `MMDiTFinalLayer`) use MMDiTJointAttention + AdaLN trio + GELUMLPFFN. No atlas contradiction; reuse is real but in a different file from what was audited.
- SYSTEM.md claim "ideogram4 reuses RMSNorm/SwiGLUFFN/Sampling/GroupNormalization": CONFIRMED — `vae.py:42` imports only `Sampling`; RMSNorm/SwiGLUFFN reuse is in `transformer.py` (the DiT block, not the VAE). Atlas claim is accurate at package level. No contradiction.

### Batch D — SSL/JEPA/encoder-decoder/segmentation (13 classes)

**Scope reconciliation:** `sam/image_encoder.py:117 PatchEmbedding` was absent from the 94-class `audit-surface.md` inventory. Confirmed as the 95th audited class via `model-import-scan.md` Cluster 6. The row is marked with `[+scope]` below.

| Class | file:line | Verdict | Target / proposed home | Drop-in? | Evidence (both sides) | Notes |
|---|---|---|---|---|---|---|
| `DINOHead` | `dino/dino_v1.py:89` | KEEP | — | — | inline: MLP stack `(in_dim→hidden_dim→...→bottleneck_dim)` + `keras.utils.normalize(L2)` + `Dense(out_dim, use_bias=False)`. `layers/heads/` `ClassificationHead` returns dict+softmax; `layers/modality_projection.py` is 1D/2D sequence projection with no L2-norm stage. No projection head with L2 pre-norm + no-bias last layer in `layers/`. | DINO SSL-specific head protocol (L2-normalize before last Dense). **BUG (9):** all sublayers built in `build()` not `__init__` — violates Keras 3 Create-vs-Build rule; breaks `from_config` for saved sub-layers. |
| `DINOv2Block` | `dino/dino_v2.py:68` | REPLACE | `layers/transformers/transformer.py:94` (`TransformerLayer`) | drop-in | inline: `norm1→attn→[ls1 LearnableMultiplier CHANNEL]→[StochasticDepth]→residual; norm2→ffn→[ls2]→[StochasticDepth]→residual`. `TransformerLayer(normalization_position='pre', use_layer_scale=True, layer_scale_init_value=init_values, use_stochastic_depth=True, stochastic_depth_rate=stochastic_depth_rate)` is bit-for-bit equivalent (confirmed: `transformer.py:225-226,344-355`). All factories (attention, ffn, norm) already delegated in both. | Replacement config: `TransformerLayer(hidden_size=dim, num_heads=num_heads, intermediate_size=int(dim*mlp_ratio), normalization_position='pre', use_layer_scale=True, layer_scale_init_value=init_values, use_stochastic_depth=True, stochastic_depth_rate=stochastic_depth_rate, ...)`. Already imports all factories correctly — the block wrapper is pure duplication. |
| `TubeMaskGenerator` | `video_jepa/masking.py:38` | RELOCATE | `layers/masking/tube_mask_generator.py` as **TubeMaskGenerator** | — | inline: stateless argsort-of-uniform-noise spatial tube mask `(B, H_p, W_p)`. No weights. Absence: `grep -rn "TubeMask\|mask_generator\|PatchMasking" layers/` → empty. Cross-SSL: identical argsort idiom cited in `masking.py:19` from `masked_autoencoder.patch_masking`. | Cross-cutting SSL primitive; reusable for MAE, V-JEPA, SimMIM, I-JEPA. Proposed `layers/masking/` subpackage alongside `PatchMasking` relocation. |
| `CausalSelfAttnMLPBlock` | `video_jepa/predictor.py:63` | REPLACE | `layers/transformers/transformer.py:94` (`TransformerLayer`) | needs-adapter | inline: pre-norm `LayerNorm→MHA(use_causal_mask=True)→x+a*gamma_a`; `LayerNorm→MLP→x+h*gamma_m`. `gamma_a/gamma_m` are per-channel LayerScale `(dim,)` initialized to `1e-5`. Uses `keras.layers.MultiHeadAttention` directly. `TransformerLayer(pre-norm, use_layer_scale, layer_scale_init=1e-5)` covers this; causal masking requires `attention_args={'use_causal_mask': True}` forwarding to factory. | Adapter: replace `keras.layers.MultiHeadAttention` with `create_attention_layer('multi_head', ...)` + `TransformerLayer(normalization_position='pre', use_layer_scale=True, layer_scale_init_value=1e-5, ...)`. Causal mask forwarding through factory unverified — flag for implementer. |
| `VideoJEPAPredictor` | `video_jepa/predictor.py:201` | KEEP | — | — | inline: 5D factorized spatial+causal-temporal stack `(B,T,H_p,W_p,D)`. Per pair: `CliffordNetBlock` (spatial, reshaped to `B*T,H,W,D`) + `CausalSelfAttnMLPBlock` (temporal, reshaped to `B*H_p*W_p,T,D`) + `CausalCliffordNetBlock` (temporal Clifford). Learned 1D temporal PE. No equivalent 5D factorized Clifford predictor in `layers/`. Sub-components: `CliffordNetBlock`, `CausalCliffordNetBlock` already from `layers/geometric/clifford_block.py`. | Bespoke V-JEPA-Clifford composite; inexpressible via any existing block. All possible sub-components maximally reuse canonical layers/. |
| `VideoJEPACliffordEncoder` | `video_jepa/encoder.py:49` | KEEP | — | — | inline: compositional glue `PatchEmbedding2D→reshape→+PositionEmbeddingSine2D (channels-first→last transpose)→N×CliffordNetBlock`. ALL sub-layers imported from `layers/`. Only inline logic is PE format adaptation (transpose). One caller. | Purely compositional; all sub-layers already canonical. The PE transpose is model-specific geometry. |
| `PatchMasking` | `masked_autoencoder/patch_masking.py:12` | RELOCATE | `layers/masking/patch_masking.py` as **PatchMasking** | — | inline: pixel-space patch masking `(B,H,W,C)→(B,H,W,C)` with learnable/noise/zero/constant mask fill; argsort sampling. Absence: `grep -rn "PatchMasking\|patch.*mask" layers/` → empty. `masked_autoencoder` has zero `layers/` imports (reuse-blind). | Cross-cutting SSL primitive: MAE/SimMIM/BEiT/MaskFeat all need pixel-space patch masking. Co-locate with `TubeMaskGenerator` in proposed `layers/masking/`. |
| `ConvDecoder` | `masked_autoencoder/conv_decoder.py:22` | KEEP | — | — | inline: N blocks of `Conv2DTranspose(2x)→[BN]→act→Conv2D(refine)→[BN]→act` + final `Conv2D(1×1)`. Returns raw tensor. `layers/heads/vision/` `DepthEstimationHead`/`SegmentationHead` add task-specific sigmoid/softmax + dict output contract. `masked_autoencoder` is reuse-blind. | MAE asymmetric decoder design (raw tensor, no task scaling). Dict output contract of vision heads blocks substitution. One caller. |
| `DPTDecoder` | `depth_anything/components.py:51` | KEEP | — | — | inline: sequential `Conv2D(3×3)→BN→act` + optional `UpSampling2D(bilinear)` + final `Conv2D`. Returns bare tensor. `layers/heads/vision/DepthEstimationHead` returns dict with sigmoid/log-depth scaling. No DPT/FPN decoder in `layers/` (confirmed: `grep -rn "DPTDecoder\|dpt.*decoder\|fpn.*decoder" layers/` → empty). **BUG (10):** all sublayers created in `build()` not `__init__` — Keras 3 Create-vs-Build violation; double-build would clobber layers via `clear()` at `components.py:159`. | Depth-Anything-specific head; raw tensor output vs dict from vision heads. Fix build anti-pattern on next touch. |
| `MaskDecoder` | `sam/mask_decoder.py:121` | KEEP | — | — | inline: SAM composite — `Embedding` IoU+mask tokens → `TwoWayTransformer` → 2×`Conv2DTranspose` 4x upsample → N hypernetwork MLPs (one per mask) → dynamic conv (token × upscaled) → IoU MLP. Multi-arg I/O `(image_embeddings, image_pe, sparse_prompt_embeddings, dense_prompt_embeddings, multimask_output)` → `(masks, iou_predictions)`. `layers/heads/vision/InstanceSegmentationHead` has BN conv head, no transformer, no dynamic conv. Uses `create_normalization_layer`. | SAM-specific composite (two-way transformer + hypernetwork MLPs + dynamic convolution). Not a general primitive. |
| `TwoWayAttentionBlock` | `sam/transformer.py:114` | KEEP | — | — | inline: 4-sublayer bidirectional block: self-attn(queries) → cross-attn(token→image) → FFN(queries) → cross-attn(image→token). I/O: `(queries, keys, query_pe, key_pe)` → `(queries, keys)`. `TransformerDecoderLayer` has 1 self-attn + 1 cross-attn — missing the 4th image-to-token cross-attn. No 4-op bidirectional block in `layers/`. Already uses `create_ffn_layer`, `create_normalization_layer`. | Bespoke SAM bidirectional cross-attention block; inexpressible via any existing `layers/` composite. Partial factory reuse is correct. |
| `PositionEmbeddingRandom` | `sam/prompt_encoder.py:88` | RELOCATE | `layers/embedding/positional_embedding_random.py` as **PositionEmbeddingRandom** | — | inline: non-trainable `(2, num_pos_feats)` Gaussian random matrix; `call(size)→grid→coords@matrix→2π→[sin,cos]→(2*D,H,W)`. `layers/random_fourier_features.py`: RBF kernel approximation on DATA space (not coordinate PE) — different application. `layers/embedding/positional_embedding_sine_2d.py:64` (`PositionEmbeddingSine2D`): deterministic sinusoidal, no random projection. Absence confirmed: `layers/embedding/` has no random-projection coordinate PE. | Random-Fourier spatial PE (NeRF-style). Reusable for SAM, NeRF, conditional diffusion with spatial prompts. Distinct from both `random_fourier_features.py` (data-space) and `PositionEmbeddingSine2D` (deterministic). |
| `PatchEmbedding` [+scope] | `sam/image_encoder.py:117` | REPLACE | `layers/embedding/patch_embedding.py:74` (`PatchEmbedding2D`) | needs-adapter | inline: `Conv2D(embed_dim, patch_size, stride=patch_size)` → output `(B, H/p, W/p, embed_dim)` **4D, not flattened**. `PatchEmbedding2D` identical Conv2D but always reshapes to `(B, N, D)` at line 247; no `flatten=False` param (confirmed: grep on `patch_embedding.py:139-260` — zero `flatten` refs). SAM needs 4D for window attention. | Adapter: add `flatten: bool = True` param to `PatchEmbedding2D`; when False, skip reshape. **Scope reconciliation:** absent from `audit-surface.md` 94-class inventory; confirmed 95th class via Cluster 6 in `model-import-scan.md`. `sam/image_encoder.py` already imports from `layers/` (ffn, norms) — the inline `PatchEmbedding` was not updated when `PatchEmbedding2D` was added to layers/. |

**Bugs noted for Synthesis appendix (Batch D):**
(9) `dino/dino_v1.py:176–267` `DINOHead.build`: all sublayers (`mlp_layers` list, `last_layer`) created in `build()` not `__init__`. Violates Keras 3 Create-vs-Build rule; breaks `from_config` for saved sub-layers.
(10) `depth_anything/components.py:149–208` `DPTDecoder.build`: all sublayers (`conv_layers`, `batch_norm_layers`, `activation_layers`, `upsample_layers`, `output_conv`) created in `build()` not `__init__`. Same anti-pattern; `self.conv_layers = []` clear at line 159 would clobber rebuilt sublayers on double-build.

### Batch E — super-res/world-model/memory/misc (20 classes)

| Class | file:line | Verdict | Target / proposed home | Drop-in? | Evidence (both sides) | Notes |
|---|---|---|---|---|---|---|
| `EDSRResidualBlock` | `thera/edsr_backbone.py:57` | RELOCATE | `layers/sr_residual_block.py` as **EDSRResidualBlock** | — | inline: `x + res_scale * conv2(act(conv1(x)))`, both Conv2D `(num_feats, k, same)`, shape-preserving. Candidate `layers/standard_blocks.py:BasicBlock:736`: 2-conv 3x3 residual but uses BatchNorm + ReLU, no `res_scale` float param. Absent: `grep -rn "res_scale\|ResidualConvBlock\|EDSRResidual" layers/` → empty. | SR-primitive-generic conv residual block with configurable `res_scale`. `res_scale=1.0` default is THERA-faithful (THERA reference ignores it); `res_scale=0.1` recovers textbook EDSR. Must document on relocate. |
| `EDSRBackbone` | `thera/edsr_backbone.py:198` | KEEP | — | — | inline: head_conv → N×`EDSRResidualBlock` → body_conv → long-skip residual. Full EDSR feature extractor; one caller. Sub-component `EDSRResidualBlock` is the RELOCATE target; once relocated Backbone imports from layers/. | Full SR feature extractor composite; THERA/EDSR-specific. |
| `RDBConv` | `thera/rdn_backbone.py:77` | RELOCATE | `layers/rdn_dense_block.py` as **RDBConv** (alongside `RDB`) | — | inline: `relu(conv(x))` concatenated onto x: `C_in → C_in + growth_rate` channels. No dense-concat conv primitive in layers/. `layers/inverted_residual_block.py` is DW+PW, no concat. Absent: `grep -rn "concatenate.*axis.*-1.*conv\|RDBConv\|DenseConv\|dense.*growth" layers/` → empty. | Atomic dense-growth conv unit for Residual Dense Networks; reusable for any RDN-family SR model. |
| `RDB` | `thera/rdn_backbone.py:166` | RELOCATE | `layers/rdn_dense_block.py` as **RDB** (alongside `RDBConv`) | — | inline: N×`RDBConv` (G0→G0+N*G channels) → 1x1 local_fusion (→G0) → residual add; I/O both G0 channels. No residual dense block in layers/. Same absence evidence as `RDBConv`. | Residual Dense Block SR primitive; standard in RDN/DenseNet-SR. Reusable for any RDN-family model. |
| `RDNBackbone` | `thera/rdn_backbone.py:289` | KEEP | — | — | inline: 2 shallow convs → D×`RDB` (collect outputs) → concat → 1x1+kxk GFF → add f_1. Full RDN feature extractor; one caller. Sub-components `RDBConv`+`RDB` are RELOCATE targets. | Full SR backbone composite; THERA-specific. |
| `TheraHypernetwork` | `thera/hypernetwork.py:84` | KEEP | — | — | inline: 1x1 `out_conv` → NEAREST-sample encoding at coords → split phi (D-008: phase first, then kernel) → compute `rel_coords` → `HeatField(rel, phi_phase, phi_kernel, t)`. D-008 locks phi split convention, NEAREST sampling, and Jacobian via `tf.GradientTape.batch_jacobian` (INV-3 exemption). Already uses `layers/thera_heat_field.HeatField` + `layers/grid_sample`. | THERA per-pixel implicit-field decoder; decision-locked (D-008 anchor at `hypernetwork.py:1-21`). No equivalent in layers/. |
| `TheraTailAir` | `thera/tails.py:85` | KEEP | — | — | inline: `return x`. Identity wrapper giving `air` size a uniform serializable interface matching `plus`/`pro` tails. No math. | Trivially thin identity; no value in layers/. |
| `_Projection` | `thera/tails.py:131` | KEEP | — | — | inline: `LayerNorm(eps=1e-6)` → `Conv2D(n_dims, 1, same)`. Private helper (prefixed `_`) for `TheraTailPlus` channel-count adaptation at block-group boundaries. No external caller. | 2-op internal helper; simple enough to be inline. Not worth extracting. |
| `TheraTailPlus` | `thera/tails.py:201` | KEEP | — | — | inline: mixed list of `_Projection` + `ConvNextV1Block` (from `layers/convnext_v1_block.py`). THERA "plus" ConvNeXt refiner with block schedule `THERA_PLUS_BLOCKS`. Already reuses `layers/convnext_v1_block.py:ConvNextV1Block`. | THERA-specific ConvNeXt refiner composite; one caller. |
| `TheraTailPro` | `thera/tails.py:360` | KEEP | — | — | inline: `conv_first(3x3)` → reflect-pad → RSTB stack (depth×`SwinTransformerBlock` from `layers/transformers/swin_transformer_block.py` + 3x3 bypass + residual) → `conv_after_body` + long-res → crop → `conv_before_upsample` → leaky-relu. D-007: reflect-pad/crop for non-window-divisible inputs. Already reuses `layers/transformers/swin_transformer_block.py:SwinTransformerBlock`. | THERA "pro" SwinIR composite; decision-locked (D-007). One caller. Sub-components maximally reuse canonical layers/. |
| `ARPredictor` | `lewm/predictor.py:22` | KEEP | — | — | inline: learned pos-embed `(1,T,D)` → optional input proj Dense → optional cond proj Dense → N×`AdaLNZeroConditionalBlock` (from `layers/transformers/adaln_zero.py`) → `LayerNorm` → optional output proj Dense. I/O: `[x, c]` pair. Already reuses canonical `AdaLNZeroConditionalBlock`. | LeWM world-model predictor composite; `[x, c]` dual-stream + pos-embed is world-model specific. One caller. |
| `ActionEmbedder` | `lewm/embedder.py:22` | KEEP | — | — | inline: `Conv1D(smoothed_dim, k=1)` → `Dense(mlp_scale*emb_dim)` → `SiLU` → `Dense(emb_dim)`. Action-sequence specific (Conv1D temporal smooth + 2-layer SiLU-MLP). No factory key for action-vector embedding. `layers/embedding/` has no Conv1D patch-embed preamble. | LeWM-specific action-embedding pipeline; robot-action semantics, one caller. |
| `MLPProjector` | `lewm/projector.py:26` | RELOCATE | `layers/ffn/normed_mlp.py` as **NormedMLPProjector**; register factory key `normed_mlp` | — | inline: `Dense(hidden)` → `[LayerNorm(eps=1e-6)]` → `GELU` → `Dense(output)`. Candidate `layers/ffn/mlp.py:MLPBlock`: `Dense(hidden) → activation → [dropout] → Dense(output)` — **no intermediate LayerNorm**. Absent: no mid-MLP LayerNorm variant in `layers/ffn/factory.py` (confirmed: 15 keys, none have post-fc1 norm). Decision D-002 (lewm/projector.py:11): LN not BN. | Standard JEPA/SSL projector pattern (I-JEPA, V-JEPA, LeWM). The `use_layer_norm` flag distinguishes it from `MLPBlock`. Reusable for any SSL pipeline. |
| `LongTermMemoryBank` | `memory_bank/memory_banks.py:32` | KEEP | — | — | inline: persistent `K_lt (S_lt,d_k)` + `V_lt (S_lt,d_v)` trainable weights; `call()` returns `(K_lt, V_lt)` ignoring input; `assign_keys_from_kmeans(centroids)` for Phase-1→2 seeding. `memory_` name prefix is load-bearing (gradient router in `WaveFieldMemoryLLM.train_step`). No equivalent in `layers/memory/` (NTM/MANN/SOM — none have persistent fixed-slot KV with offline KMeans seeding). | WaveFieldMemoryLLM-specific persistent KV bank; name-prefix gradient routing + KMeans seeding are co-designed with parent model. No overlap with layers/memory/ NTM/MANN/SOM families. |
| `WorkingMemoryBank` | `memory_bank/memory_banks.py:193` | KEEP | — | — | inline: `W_K (use_bias=False)` + `W_V (use_bias=True)` Dense projections `X_W(B,T,D)→(K_wm,V_wm)`; `memory_wm_` name prefix load-bearing; `d_k!=d_v` + `d_v<embed_dim` blueprint constraints. No equivalent stateless KV-projector with name-prefix gradient routing in `layers/memory/`. | Parent-model-specific stateless KV projector; name prefix + constraints are training-protocol-specific. No overlap with NTM family. |
| `MemoryReadController` | `memory_bank/read_controller.py:43` | KEEP | — | — | inline: multi-head top-K STE retrieval (W_Q → per-head Q → concat K_total=[K_lt_tiled,K_wm] → similarity → causal+pad mask on WM slice only → softmax → STE hard_w → retrieve V → head-concat → W_out+LayerNorm → per-channel gate sigmoid(W_g)) + 6 optional anti-collapse aux losses (gate-entropy, load-balance, z-loss, diversity, v-diversity, InfoNCE). 7-positional-arg call (D3: Functional-API incompatible). M_static baked at construction for XLA. `layers/memory/NTMReadHead`: content/location addressing — different paradigm. | Bespoke STE top-K memory read with gated injection; no equivalent in layers/memory/. Inexpressible via NTM addressing. |
| `MemoryWriteController` | `memory_bank/write_controller.py:29` | KEEP | — | — | inline: wraps `WorkingMemoryBank` → right-pads K_wm/V_wm to `max_seq_len` via concatenation (not ops.pad — TF tracing workaround, `write_controller.py:101-106`) → builds padding mask. `tf.debugging.assert_less_equal` for T≤max_seq_len guard. | Glue layer composing WorkingMemoryBank + zero-padding + mask; TF-backend workaround decisions make this non-portable. One caller. |
| `NAMCell` | `nam/cell.py:127` | KEEP | — | — | inline: `initialize_carry(batch_size)→dict` + `call((carry,hidden,mask,token_ids))→(new_carry,outputs_dict)`. **Not an RNN cell** (no `state_size`/`output_size`/`get_initial_state`). Architecture: `GroupAttention`+`TreeMHA` (from `layers/tree_transformer/model`) + `NTMMemory`+`NTMReadHead`+`NTMWriteHead` (from `layers/memory/baseline_ntm`) + norms/FFN factories + FIXED arithmetic ops (`_fixed_add/sub/mul/div`, `cell.py:43-68`) + halt head. `layers/logic/LearnableArithmeticOperator`: individual learnable op — different abstraction. Sub-components maximally reuse layers/. | NAM-specific multi-step arithmetic expression reduction; FIXED arithmetic (not learned), NTM + tree-transformer coupling. All sub-components already canonical. |
| `TRMReasoningModule` | `tiny_recursive_model/components.py:47` | REPLACE | `layers/reasoning/hrm_reasoning_module.py:57` (`HierarchicalReasoningModule`) | drop-in | inline: `hidden_states + input_injection` → N×`TransformerLayer` (from `layers/`) → output same shape. Candidate `HierarchicalReasoningModule`: same injection mechanism (`hidden_states + input_injection`) → N×`TransformerLayer` → output. Both accept identical params: `(hidden_size, num_heads, expansion, num_layers, seq_len, puzzle_emb_len, rope_theta, attention_type, ffn_type, normalization_type, normalization_position, dropout_rate, attention_dropout_rate)`. Bit-for-bit equivalent. | `TRMInner` creates `H_level`/`L_level` as `TRMReasoningModule`; replace with `HierarchicalReasoningModule` — same parameter names, same behavior. `tiny_recursive_model` already imports from `layers/transformers/`; add import from `layers/reasoning/`. |
| `TRMInner` | `tiny_recursive_model/components.py:296` | KEEP | — | — | inline: `token_emb (Embedding)` + `H_level (TRMReasoningModule)` + `L_level (TRMReasoningModule)` + `lm_head Dense(vocab_size, no_bias)` + `q_head Dense(2)` + learnable `H_init (1, seq, D)` + `L_init (1, seq, D)`. `call(carry, data, training)`: embed → zero-pad puzzle prefix → `z_L=L_level(z_L,emb)` → `z_H=H_level(z_H,z_L)` → stop_gradient carry → lm_head + q_head → `(q_halt, q_continue)`. `layers/reasoning/HierarchicalReasoningCore`: runs multiple inner cycles (h_cycles*l_cycles) with ACT loop — different abstraction level (outer loop vs single inner step). | TRM-specific ACT inner step; learnable init states + ACT halt/continue outputs + puzzle-prefix zero-padding are TRM-specific. After `TRMReasoningModule` → `HierarchicalReasoningModule` swap, update imports only. |

**Bugs noted for Synthesis appendix (Batch E):**
(11) `memory_bank/write_controller.py:111-117`: uses `tf.debugging.assert_less_equal` directly — raw TF op, backend-portability limiter. Documented as intentional TF-backend workaround for `ops.pad` dynamic-paddings tracing failure. Not a functional bug on TF backend but breaks JAX/PyTorch backends. Flag for future multi-backend portability work.
